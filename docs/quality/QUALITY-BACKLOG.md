# QUALITY-BACKLOG — 마스터 품질 백로그 (SSOT)

> 품질 발견의 단일 적재처. 신규 기능은 프로젝트 TODO, 품질 결함/마찰은 여기.
> 실사용자 피드백도 같은 스키마로 여기에.
> 스키마: `- [ ] **QS-<연번> <제목>** — P0|P1|P2 · 저니 J# · 화면 · 기준 <카테고리>` + 증상 1-2줄 + (발견 출처).
> P0=저니 차단/데이터 오류 · P1=명백한 마찰 · P2=폴리시 · ★=제품 결정 필요(라운드에서 옵션 제시).

## P0 — 차단/데이터 오류

## P1 — 명백한 마찰

- [ ] **QS-13 댓글 알림 딥링크 `?rec=`가 오래된 글에서 무반응** — P1 · 저니 —(미정: 깅스타그램·알림 복귀) · `/story`(홈) · 기준 6.이음새
  "내 운동기록에 새 댓글" 알림을 눌러도 첫 16건 밖의 글이면 릴스가 안 열리고 전광판 맨 위만 뜬다(주소는 살아 있는데 화면이 안 뜨는 상태). RPC에 단건 조회 경로가 없어 목록에 없으면 못 연다. 기록이 쌓일수록 발생 확률이 오르는 잠복 결함.
  (기강이야기 홈탭 점검 2026-07-29 · 상세: `docs/quality/2026-07-29-기강이야기-홈탭-점검.md`)

- [ ] **QS-14 서버 렌더 KST 어긋남 — 새벽 날짜·D-day 오표기 + 하이드레이션 불일치** — P1 · 저니 J1 · `/story`(홈) 외 공용 · 기준 2.신뢰
  tz 없는 `dayjs()`로 "오늘"을 판정하는 곳 7군데(제호 날짜·D-day·30일 창·월 라벨·NEW 배지 등). Vercel=UTC라 KST 00~09시엔 서버 HTML이 하루 밀려 그려지고 하이드레이션이 깨진다. `getRaceDday`·`isNewRecord`는 프로필 카드 등 지면 밖에서도 공용 — 전수 적용 필요.
  (기강이야기 홈탭 점검 2026-07-29)

- [ ] **QS-15 피드 payload의 71%가 화면이 안 쓰는 데이터** — P1 · 저니 J1 · `/story`(홈) · 기준 1.첫인상
  `get_team_story_feed` 44.9KB 중 `actv_rank`가 31.9KB(68명 전원의 최고기록·러닝프로필). 실제 소비는 리드 대표 **1명의 칭호·소개**뿐이고 무더기는 이름·점수만 쓴다. dynamic 페이지라 매 방문 전송. RPC에서 상위 3명만 부품을 채우면 앱 코드 변경 없이 약 28KB 절감(클라 타입이 이미 전부 옵셔널).
  (기강이야기 홈탭 점검 2026-07-29 · dev 실측)

- [ ] **QS-10 배포 후 첫 전체 재계산의 잔액 변동 사전 확인 (일회성 운영 절차)** — P1 · 저니 J2 · `/admin/dues` · 기준 2.신뢰
  리플레이 전환(f0b309e)은 과거 증분 시대에 눌어붙은 오차를 자가 치유하므로 **첫 재계산에서 일부 회원 잔액이 말없이 바뀐다**. dev 전수 대조에서 2명 확인: ①06-06 옛 재계산이 근거 이력 없이 +2,000 가산(교정됨) ②은행거래 미연결 수동납부 2,000을 구코드가 합산 누락(교정됨). prd 배포 직후 원장 스냅샷을 떠두고 '전체 재계산' 후 diff를 운영자가 눈으로 승인할 것.
  (수정 라운드 #1 정합성 리뷰 2026-07-02)

## P2 — 폴리시

- [ ] **QS-16 응원 실패분이 세션 내내 부풀려 보임** — P2 · 저니 J1 · `/story`(홈) · 기준 2.신뢰
  저장 실패 시 버튼 로컬 카운트만 롤백하고 부모가 sessionStorage에 쌓는 몫(`myBumps`)은 그대로라, 슬롯이 한 바퀴 돌아 재마운트되면 실패분이 되살아난다. 비로그인은 모든 탭이 실패 + `settled` 판정이 구조적으로 거짓이라 누를수록 계속 커진다.
  (기강이야기 홈탭 점검 2026-07-29)

- [x] **QS-17 ★ 비로그인에게 릴스 댓글이 항상 "0개"** — P2 · 저니 J1 · `/story`(홈) · 기준 2.신뢰
  `cmnt_mst` RLS가 SELECT까지 `authenticated` 전용이라 anon은 에러 없이 0행 → "댓글 없음"과 구분 불가(dev 기준 34건/12개 글이 전부 0으로 보임). **결정 (A)**: 댓글은 크루 내부용으로 확정, RLS는 그대로 두고 화면이 그 사실을 밝히게. 하단 입력 줄을 "로그인하고 댓글 보기"로 바꾸고 비로그인 조회·구독을 껐다. 시트는 이미 로그인 안내가 있어 손대지 않음.
  (기강이야기 홈탭 점검 2026-07-29 · 결정·수정 2026-07-29)

- [ ] **QS-18 격자 첫 조회가 1080px 원본 그대로 (썸네일 — 조건부 보류)** — P2 · 저니 J1 · `/story`(홈) · 기준 3.모바일
  ~160px 칸에 원본(평균 181KB)이 내려온다. **1차 대응은 캐시 헤더**(`cacheControl` 1년)로 끝냈다 — 기본값이 1시간이라 재방문마다 16장을 다시 받고 있었고, 실제 트래픽 대부분이 그쪽이다. Supabase 이미지 변환은 **prd에서 403(FeatureNotEnabled)**·무료 월 100회라 탈락, q80 하향은 인스타 내보내기 원본이라 기각. **조건**: 출시 후 Egress가 3GB(한도 60%)에 닿으면 그때 썸네일 2장 저장을 얹는다(캐시=재방문 / 썸네일=첫 조회라 겹치지 않아 나중에 추가해도 손해 없음). 현재 prd Egress 0.525GB/5GB, post-photos 파일 0개(미출시).
  (기강이야기 홈탭 점검 2026-07-29 · 캐시 대응 2026-07-29)

- [ ] **QS-19 비로그인 응원이 낙관 반영 후 에러 토스트(같은 지면 댓글과 정책 불일치)** — P2 · 저니 J1 · `/story`(홈) · 기준 4.인터랙션
  비로그인 응원은 구조적으로 100% 실패인데 UI는 숫자·이모지로 성공처럼 반응한 뒤 토스트만 띄운다. 댓글은 로그인 CTA로, 기록 올리기는 버튼 숨김으로 처리하는데 응원만 다르다. 고치면 QS-16의 비로그인 케이스도 함께 사라진다.
  (기강이야기 홈탭 점검 2026-07-29)

- [ ] **QS-20 릴스·프로필 카드가 열려 있어도 리드가 계속 자동 전환** — P2 · 저니 J1 · `/story`(홈) · 기준 4.인터랙션
  정지 스위치가 `paused`(손 닿음)·`hidden`(탭 숨김)뿐이라 같은 문서 안 다이얼로그는 못 잡는다. 릴스를 보다 닫으면 보던 슬롯이 바뀌어 있다(한 바퀴 돌았으면 랜덤 재추첨까지). 스크롤로 리드가 화면 밖일 때도 같은 스위치로 처리 가능.
  (기강이야기 홈탭 점검 2026-07-29)

- [x] **QS-21 기강이야기 폴리시 묶음 9건 → 7건 수정 · 2건 보류** — P2 · 저니 J1 · `/story`(홈) · 기준 복합
  수정: 빈 상태 판정(posts·ghosts 기준으로 교정) · 주석 부패 · 죽은 payload(`week_stat`·`month_rank` 제거 → CTE 5개 동반 제거) · 아바타 rAF 오프스크린 정지 · 릴스 `content-visibility`(가상화 대신) · `?rec=` 잔류 · 깅스타그램 빈 상태. **`?rec=`는 원인이 따로 있었다** — `MiniCalendar`의 `clearDeepLinkParams` 키 목록에 `rec`가 빠져 있었고(그 함수 주석이 경고하던 누락), 사본을 `lib/notifications/deep-link.ts`로 옮겨 드리프트를 구조적으로 막았다.
  보류: **카피**는 사용자가 정한 문구라 판단 대상 아님 · **응원 유실**은 서버 액션이라 `keepalive` 불가(전용 Route Handler 필요), 잃는 양 대비 비용이 커 보류.
  (기강이야기 홈탭 점검 2026-07-29 · 처리 2026-07-29)

- [x] **QS-22 리드 사진 슬롯이 빈 `src`를 그려 페이지를 이미지로 재요청** — P2 · 저니 J1 · `/story`(홈) · 기준 2.신뢰
  `story-lede.tsx`의 `src={lede.photo.url ?? ""}`를 형제(격자·릴스)와 같게 — 값이 없으면 `<Image>`를 아예 안 그린다.
  (QS-13 수정 중 발견 2026-07-29 · 처리 2026-07-29)

- [ ] **QS-22 리드 사진 슬롯이 빈 `src`를 그려 페이지를 이미지로 재요청** — P2 · 저니 J1 · `/story`(홈) · 기준 2.신뢰
  `story-lede.tsx:1081`의 `src={lede.photo.url ?? ""}` — 저장소에서 Image src에 `?? ""`를 쓰는 유일한 곳이고, 형제(격자·릴스)는 정확히 이걸 하지 말라고 주석에 적어 두고 값이 없으면 안 그린다. 평소엔 RPC의 사진 필터가 가려 주지만 `unstable_cache`에 남은 옛 payload가 내려오면 드러난다(dev 서버 재시작 첫 렌더에서 실제 경고 확인).
  (QS-13 수정 중 발견 2026-07-29)

- [ ] **QS-12 앵커 불변식이 코드·제약으로 강제되지 않음 (잠복)** — P2 · 저니 J2 · 재계산 · 기준 2.신뢰
  리플레이의 base는 "앵커 잔액에 반영(rflt_yn=true) 면제가 녹아 있지 않다"에 의존. 현재 데이터는 안전(모든 반영 면제가 앵커 이후 생성) — 향후 면제가 녹은 시드 스냅샷을 새로 만들면 이중 계상 재발. `LEDGER_EPOCH` 이전 스냅샷을 새로 만들지 말 것. 상세: `.claude/docs/KNOWLEDGE.md` 앵커+리플레이 절.
  (수정 라운드 #1 정합성 리뷰 2026-07-02)

## 완료
(수정 라운드에서 체크오프 시 여기로 이동, 커밋 SHA 병기)

- [x] **QS-1 은행 엑셀 업로드 진입점 소실** — P0 → 인박스 헤더에 업로드 버튼+결과 요약 복구 (f0b309e)
- [x] **QS-2 ★ 확정된 거래의 관리자 조회 화면 부재** — P1 → 결정: 인박스 '처리됨' 필터. 확정일시·확정자·검색, 최근 200건+잘림 고지, 조회 실패는 핵심 저니와 격리 (f0b309e)
- [x] **QS-3 ★ 확정 취소 UI 부재** — P1 → 결정: 개별 거래 취소만(파일 롤백 제외). 처리됨 행 '취소'(confirm) → 인박스 복귀+납부 cancelled+회원 잔액 복구 (f0b309e)
- [x] **QS-4 cancelTransaction 가드 시맨틱 버그** — P1 → 근본 수정: 재계산을 증분(커서)에서 **앵커+전체 리플레이**로 전환(멱등·자가치유, dev 161명 전수 대조 검증). 앵커(개시잔액) 이전 거래만 취소 거부 (f0b309e)
- [x] **QS-5 확정 루프 부분 실패 시 보상 없음** — P1 → 실제 반영된 행만 추적(`select` 반환)해 전 구간 보상 롤백. 동시 확정 경합 시 남의 확정 롤백·원장 이중행도 함께 차단 (f0b309e)
- [x] **QS-6 일괄 확정 범위 고지 부족** — P1 → 확정 전 버킷별 요약 다이얼로그("지금 보는 필터와 관계없이 전체 확정") + 취소 가능 고지 (f0b309e)
- [x] **QS-8 rollback-snapshot의 `new Date()` TZ 버그** — P2 → 리플레이 전환으로 불건전해진 rollback-snapshot/rollback-xlsx 액션 자체를 삭제 (f0b309e)
- [x] **QS-9 납부 0건 회원 커서가 now로 저장돼 과거 입금이 업로드 컷오프에 소실** — P0급(수정 라운드 중 발견) → 커서를 앵커 커서/가입월초로 폴백, 수동납부는 커서를 밀지 않음. 면제 rule 유니크 인덱스(20260702120000)로 동시 재계산 이중 적재도 차단 (f0b309e)
- [x] **QS-7 잔액 원장 → 개인 납부 근거 드릴다운** — P2 → `/admin/dues/members/[memId]` 관리자 상세(납부·취소·면제·기타, 본인 화면과 동일 조립 규칙) + 원장 행 이름 링크 (0aff38f)
- [x] **QS-11 ★ 고아 액션 2종 처분** — P2 → 결정: 재연결. 인박스 '수동 등록' 다이얼로그(FEE_ITEM_CD 전체 분류) + 미확정 행 '삭제'(confirm, 소프트 삭제로 재업로드 재유입 차단) (0aff38f)
