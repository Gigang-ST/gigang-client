# 칭호획득 리드 슬롯 v2 — 사람 대표 + 하단 명단

> 2026-08-12 · v1(칭호별 묶음 나열)을 dev 실데이터 화면으로 본 뒤 갈아탄 개정판.
> **v1 스펙**(2026-08-07-칭호획득-리드슬롯-design.md)의 지면 설계·응원 결정을 이 문서가
> 대체한다. **데이터 설계(RPC·쿼리·캐시)는 v1 그대로다 — 마이그레이션 없음.**

## 배경 — v1이 지면에서 실패한 이유

v1은 "수십 명이 한꺼번에 따는 sweep이 이 슬롯의 존재 이유"라 보고 칭호별 묶음 나열을
골랐다. dev 실데이터로 화면을 세워 보니 세 문제가 겹쳤다(사용자 피드백: "휑하고,
아바타만 있어서 누군지도 모르겠다"):

1. **이름이 지면에 하나도 없다.** 24px 아바타만 나열되는데 프사 미설정 멤버는 DiceBear
   랜덤 얼굴이라 사실상 익명이다. 다른 슬롯은 전부 이름이 주연이다.
2. **평상시 데이터가 sweep 가정과 다르다.** 칭호당 1~2명짜리 줄이 대부분이라 얇은 줄
   3개의 오른쪽이 텅 빈다. sweep은 이벤트고, 지면은 평상시에 서 있어야 한다.
3. **응원이 없다.** "여러 칭호 × 여러 사람이라 대상 불성립"으로 뺐는데, 그 결과 footer
   오른쪽까지 비어 휑함을 더한다. 나머지 여섯 슬롯은 모두 응원이 있다.

## 결정 요약

| 쟁점 | 결정 | 근거 |
|------|------|------|
| 슬롯 주인공 | **사람 대표 1명(회전) + 하단 얼굴+이름 칩 명단** | 다른 사람 슬롯(활동지수·목표)과 같은 문법으로 회귀. sweep 가시성은 하단 명단이 잇는다 |
| 우측 레일 | **기각** | 은퇴한 문법(story-lede.tsx buildLedes 주석 — "우측 레일 대신 시간으로 모두에게 자리를 준다"). 이 슬롯만 레일이면 새 어법 발명 |
| 회전·명단 단위 | **사람** — 사람별 최신 수여 1건으로 dedupe | 한 사람이 칭호 셋을 동시에 따도 대표 3연속·명단 3중복이 안 되게. 목표 팻말 `dedupePledgesByMember`와 같은 원칙 |
| 헤드라인 | `{이름}, 새 칭호를 달다` | 칭호명을 넣으면 을/를 조사가 칭호마다 갈린다(山神을/HALF를). 칭호명은 배지가 실물로 말한다. 문구는 관례대로 화면 보고 다듬기 |
| 대표 소개 줄 | **`ttl_desc`**(칭호 설명) | "기강 남자 10K 1위" 같은 설명이 곧 이 사람이 해낸 일이다. `intro_txt`를 실으려면 마이그레이션이 따라와 기각(YAGNI) |
| 응원 | **부활** — `actv` + 대표 mem_id | 대표가 한 명이라 대상이 성립한다. 활동지수·목표·깅스타그램과 같은 멤버 기준 카운터(같은 사람이 여러 슬롯 대표면 🔥 합산 — 기존 의도) |
| RPC | **변경 없음** | 기존 payload(이름·아바타·ttl_desc·visibility·grant_cnt)로 전부 만들 수 있다 |

## 지면 설계

```
┌──────────────────────────────┐
│ 기강에 새 역사를 쓰다         │ ← kicker (유지)
│                              │
│ 홍기강, 새 칭호를 달다        │ ← 헤드라인 (hero: "headline")
│                              │
│  ◉ 홍기강 [山神✨]            │ ← 대표: Avatar(lg) + 이름 + 새 칭호 배지
│  "기강 트레일런 PB 남녀 1위"  │ ← ttl_desc (visibility 게이트, 없으면 줄 생략)
│  ────────────────────        │ ← 괘선
│  ◯한세희 ◯함용석 ◯기지현 …   │ ← 나머지 획득자: 얼굴+이름 칩 wrap,
│                              │    넘치면 이 영역만 스크롤 (대회 명단 문법)
│ 획득 143건            🔥 12  │ ← footer: 총 수여 건수 + 응원(부활)
└──────────────────────────────┘
```

- **대표 블록**: `Avatar`(lg) + 이름(bold) + `TitleBadge`(그 사람의 **이번 창 최신 수여
  칭호**, effect `null` — 이펙트는 사람 소유라는 v1 결정 유지, 탭하면 기존 설명 툴팁
  그대로 — `heldByMe` 근사 판정도 유지). 아래 `ttl_desc` 한 줄 — 노출 게이트는 툴팁과
  같은 규칙(`desc_visibility`: always/others는 노출, held는 내가 보유 시, never는 비노출).
  설명이 없거나 게이트에 걸리면 줄째 생략. 얼굴·이름 탭 → 프로필 카드.
- **하단 명단**: 대표를 뺀 나머지 획득자(사람 dedupe 후)를 **얼굴+이름 칩**으로 wrap
  나열 — 대회 roster의 칩 마크업 재사용(xs 아바타 + 13px 이름, 탭 → 프로필 카드).
  넘치면 명단 영역만 `overflow-y-auto`(대회 명단과 같은 동작 — 헤드라인이 2줄로 늘어도
  잘리는 대신 이 영역이 줄어든다). **`외 N`은 쓰지 않는다** — RPC가 칭호당 10건으로
  자르므로 "사람 수" 전체를 정확히 셀 수 없고(칭호 간 중복 인물도 있다), 총량은 footer
  `획득 {G}건`이 이미 말한다.
- **footer**: 왼쪽 `획득 {G}건`(v1 그대로 — `grant_cnt` 합산). 오른쪽 응원 버튼 —
  `buildEntity("actv", 대표.mem_id, "fire")`.
- **스와이프 순서·빈 상태**: v1 유지(`ORDER` 맨 뒤 `title`, pool 0이면 슬롯 없음).

### 회전 규칙 (v1의 "3개 페이지 전진" 폐기)

- pool = grants 평탄화 → **사람별 최신 수여 1건 dedupe** → `grnt_at` 내림차순. 길이 P.
- 대표 = `rotate(pool, titlePick)[0]`. 명단 = pool − 대표, **최신순 고정**(회전과 무관 —
  매 바퀴 명단까지 뒤섞이면 어지럽다).
- `initialTitlePick`은 서버가 뽑는다(v1 유지, 이제 P 기준). 한 바퀴 완주마다
  `titlePick += 1`(v1의 `+= 3` 대체) — 모든 획득자가 돌아가며 대표가 된다.
- sweep 칭호는 RPC 상한 때문에 최신 10명까지만 pool에 든다 — 대표 회전도 그 안에서
  돈다. 허용(총량은 footer가 전달).

### 264px 예산

kicker + 헤드라인(1~2줄) + 대표 블록 + 괘선 + 명단(가변·스크롤) + footer. 명단이
`flex-1 + overflow-y-auto`라 세로 예산 초과가 구조적으로 없다(대회 슬롯과 동일).
**실측은 375px 기기 에뮬레이션**(DESIGN.md 경고 — 창 줄이기 방식 금지), 화면 확인은
사용자 몫(관례).

## 응원 검증 확장 (구현 필수)

`bump-reaction`의 `actv` 검증 출처가 현재 3개(활동지수 랭킹 · 목표 팻말 · 깅스타그램
작성자)다. **네 번째 출처로 칭호획득 pool(`story-titles` 캐시)을 추가**한다 — 검증은
"이 mem_id가 지면에 서 있는가"이므로 대표만이 아니라 **pool 전체**(회전으로 누구든
대표가 된다)를 본다. 빼먹으면 대표에게 응원을 누를 때 서버가 거부한다.
DESIGN.md의 "세 출처" 서술도 함께 갱신한다.

## 연결 지점 (구현 개요)

1. `lib/story-title.ts` — `rotateTitlePage`·`TITLE_LEDE_PAGE`·`TITLE_ROW_FACES` 제거,
   평탄화+사람 dedupe 헬퍼 신설(서버 `initialTitlePick` 추첨과 클라이언트가 공유).
   테스트(`lib/__tests__/story-title.test.ts`) 재작성 — dedupe(사람별 최신 1건)·회전
   전체 커버·명단 순서(최신순, 대표 제외)를 못박는다.
2. `story-lede.tsx` — `Lede.titleRoster` → 대표+명단 형태로 교체, `buildLedes` ⑦ 블록·
   전용 렌더 분기 재작성, `rerollAllPicks`의 `+= TITLE_LEDE_PAGE`를 `+= 1`로.
3. `app/actions/story/bump-reaction.ts` — `actv` 검증 출처에 칭호획득 pool 추가.
4. `story/page.tsx` — `initialTitlePick` 추첨 기준을 P(사람 pool 길이)로.
5. DESIGN.md — StoryLede 슬롯 서술·actv 검증 출처 갱신. v1 스펙 상단에 supersede 노트.

## 범위 밖 (이번 작업 아님)

- v1과 동일: 중규모 작업 2번(신규 칭호 대량 추가), 3번(획득 시 대표 칭호 자동 변경 —
  현재는 "대표가 아예 없으면 첫 수여분을 대표로" 폴백만 있다, `lib/titles/engine.ts`),
  4번(이펙트 해금).
- `intro_txt`를 RPC에 싣기(마이그레이션) — ttl_desc로 부족해지면 그때.
- 명단 전체 뷰·릴스식 상세.

## 검증

- `pnpm run lint` / `pnpm exec vitest run lib/__tests__/story-title.test.ts` / `tsc --noEmit`.
- 375px 에뮬 실측(사용자 QA): ① 대표 블록 + 명단 2줄 + 헤드라인 2줄 조합 ② 긴 칭호명
  배지에서 이름·설명 생존 ③ 명단 스크롤 동작.
- 응원: 대표에게 눌러 서버 반영(400 아님) 확인 — 검증 4번째 출처가 실제로 뚫리는지.
