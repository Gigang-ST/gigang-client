# 모임 대기열 임박 구간 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모임 시작 2시간 전부터 대기열 자동 승급을 멈추고 선착순으로 전환하며, 일정탭 다이얼로그에 대기 명단과 내 순번을 노출한다.

**Architecture:** 시간 경계(`GATHERING_WAITLIST_OPEN_HOURS = 2`)를 순수함수 하나로 정의하고, DB(`promote_gthr_waitlist` 게이트)와 앱(알림 발송 판정·버튼 라벨) 양쪽이 같은 기준을 본다. 대기 명단은 RPC를 건드리지 않고 세션 클라이언트로 `gthr_wait_rel`을 직접 조회해 RLS가 방어하게 한다.

**Tech Stack:** Next.js App Router / React 19 / TypeScript / Supabase(plpgsql RPC, RLS) / vitest

**Spec:** `docs/superpowers/specs/2026-09-11-모임-대기열-임박구간-design.md`

## Global Constraints

- 패키지 매니저는 `pnpm`. 테스트는 `pnpm vitest run <경로>`.
- 날짜/시간은 `import { dayjs, parseEventTime } from "@/lib/dayjs"` 만 사용. `new Date()` 금지. timestamptz 표시는 `formatKST`.
- 마이그레이션 첫 줄은 `SET lock_timeout = '3s';` (`supabase/migrations/README.md` 규약). 파일 끝에 REVERT 주석 블록.
- 화면 문구는 확정본 그대로 쓴다(§4-2). 임의로 고치지 않는다:
  - 2시간 전까지 확인 다이얼로그 4줄: `참석자가 취소하면 순번대로 자동 참석됩니다.` / `참석이 어려우면 미리 대기를 취소해주세요.` / `시작 2시간 전부터는 대기 순번이 없고 선착순으로 바뀝니다.` / `이후 빈 자리가 나면 알림을 보내드리니 직접 참석해주세요.`
  - 2시간 이후 확인 다이얼로그 2줄: `지금은 순번 없이 선착순입니다.` / `빈 자리가 나면 알림을 보내드립니다.`
  - 버튼: `대기 신청` / `대기 취소` / `빈 자리 알림 요청` / `알림 요청 취소`
  - 상태 문구: `대기 3번 · 자리 나면 자동 확정` / `빈 자리가 나면 알려드려요`
  - 알림: 제목 `'{모임명}' 빈 자리가 났어요`, 본문 `지금은 순번 없이 먼저 누르는 분이 참석하실 수 있어요.`
- DB·내부 식별자는 `gthr_wait_rel` · `waiting` 그대로. 새 상태값을 만들지 않는다.
- 타이포그래피는 `components/common/typography.tsx` 컴포넌트 사용(매직넘버 금지), 색은 CSS 변수 토큰만.
- 커밋은 각 Task 끝에서. **push·PR 생성은 하지 않는다**(사용자 요청 시에만).

---

### Task 1: 2시간 경계 순수함수

**Files:**
- Modify: `lib/gathering/cancel-imminent.ts`
- Test: `lib/gathering/__tests__/cancel-imminent.test.ts`

**Interfaces:**
- Consumes: `parseEventTime`, `dayjs` (`@/lib/dayjs`)
- Produces: `GATHERING_WAITLIST_OPEN_HOURS: number`, `isWaitlistOpenToAll(sttAt: string, now?: dayjs.Dayjs): boolean`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/gathering/__tests__/cancel-imminent.test.ts` 하단에 추가:

```ts
import { GATHERING_WAITLIST_OPEN_HOURS, isWaitlistOpenToAll } from "@/lib/gathering/cancel-imminent";

describe("isWaitlistOpenToAll — 시작 2시간 전부터 대기 순번이 없어진다", () => {
  const STT = "2026-09-11T10:00:00Z"; // KST 19:00

  it("2시간보다 더 남았으면 false(아직 대기 순번이 산다)", () => {
    expect(isWaitlistOpenToAll(STT, dayjs("2026-09-11T07:59:00Z"))).toBe(false);
  });

  it("정확히 2시간 남았으면 false — 경계는 미만일 때만 열린다", () => {
    expect(isWaitlistOpenToAll(STT, dayjs("2026-09-11T08:00:00Z"))).toBe(false);
  });

  it("2시간 미만이면 true", () => {
    expect(isWaitlistOpenToAll(STT, dayjs("2026-09-11T08:00:01Z"))).toBe(true);
  });

  it("이미 시작했으면 true", () => {
    expect(isWaitlistOpenToAll(STT, dayjs("2026-09-11T11:00:00Z"))).toBe(true);
  });

  it("상수는 2다 — 취소 사유 경계(5)와 다른 값이어야 한다", () => {
    expect(GATHERING_WAITLIST_OPEN_HOURS).toBe(2);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm vitest run lib/gathering/__tests__/cancel-imminent.test.ts`
Expected: FAIL — `isWaitlistOpenToAll is not a function`

- [ ] **Step 3: 구현**

`lib/gathering/cancel-imminent.ts` 맨 아래에 추가:

```ts
/**
 * 대기 순번이 사라지고 선착순으로 열리는 경계.
 *
 * 취소 사유 경계(GATHERING_CANCEL_IMMINENT_HOURS = 5)와 **성격이 다르다** — 저쪽은
 * "이 취소가 남에게 피해를 주는가", 이쪽은 "줄 순서를 지킬 만큼 시간이 남았는가"다.
 * 값을 공유하지 않되 같은 파일에 나란히 둔다: "임박"의 정의가 흩어지면 한쪽만 바뀐다.
 *
 * 설계: docs/superpowers/specs/2026-09-11-모임-대기열-임박구간-design.md §2
 */
export const GATHERING_WAITLIST_OPEN_HOURS = 2;

/**
 * 이 모임이 "대기 순번 없이 선착순" 구간에 들어갔는가.
 *
 * true 면 ① 자동 승급을 하지 않고 ② 대기 신청이 "빈 자리 알림 요청"의 뜻이 되며
 * ③ 대기 중인 사람의 버튼이 "참석하기"로 바뀐다.
 *
 * 경계는 **미만**이다 — 정확히 2시간 남은 시점은 아직 대기 순번이 산다
 * (isCancelReasonRequired 와 같은 방향으로 맞춘다).
 *
 * @param sttAt 모임 시작 시각(UTC ISO 문자열, gthr_mst.stt_at)
 * @param now 기준 시각(테스트용, 생략 시 현재 시각)
 */
export function isWaitlistOpenToAll(sttAt: string, now: dayjs.Dayjs = dayjs()): boolean {
  return parseEventTime(sttAt).diff(now, "hour", true) < GATHERING_WAITLIST_OPEN_HOURS;
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm vitest run lib/gathering/__tests__/cancel-imminent.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/gathering/cancel-imminent.ts lib/gathering/__tests__/cancel-imminent.test.ts
git commit -m "feat(gathering): 대기 순번이 사라지는 2시간 경계 판정 추가"
```

---

### Task 2: 버튼 라벨·안내 문구 분기

**Files:**
- Modify: `lib/gathering/waitlist.ts`
- Test: `lib/gathering/__tests__/waitlist.test.ts`

**Interfaces:**
- Consumes: `AttendState` (기존)
- Produces:
  - `attendButtonLabel(state: AttendState, isFull: boolean, openToAll: boolean): string`
  - `waitHintText(state: AttendState, rank: number | null, total: number, openToAll: boolean): string | null`
  - `waitConfirmCopy(openToAll: boolean): { title: string; lines: string[]; confirmLabel: string }`

> ⚠️ 앞 두 함수는 **인자가 하나 늘어난다.** 호출부가 셋이다 —
> `gathering-attend-button.tsx`, `gathering-detail-dialog.tsx`, 그리고 이 테스트.
> Task 8·9에서 호출부를 고치므로 이 태스크만으로는 `tsc`가 깨진다(정상).

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/gathering/__tests__/waitlist.test.ts` 의 기존 `attendButtonLabel`·`waitHintText` describe 블록을 아래로 **교체**하고, `waitConfirmCopy` 블록을 추가한다:

```ts
describe("attendButtonLabel — 만석은 막지 않고 대기로 잇는다", () => {
  it("참석 중이면 구간과 무관하게 ✅ 참석", () => {
    expect(attendButtonLabel("attending", false, false)).toBe("✅ 참석");
    expect(attendButtonLabel("attending", true, true)).toBe("✅ 참석");
  });

  it("자리가 있으면 참석하기", () => {
    expect(attendButtonLabel("none", false, false)).toBe("참석하기");
  });

  it("만석이면 대기 신청", () => {
    expect(attendButtonLabel("none", true, false)).toBe("대기 신청");
  });

  it("대기 중이면 대기 취소", () => {
    expect(attendButtonLabel("waiting", true, false)).toBe("대기 취소");
  });

  it("선착순 구간에서 만석이면 '대기'라고 부르지 않는다 — 빈 자리 알림 요청", () => {
    expect(attendButtonLabel("none", true, true)).toBe("빈 자리 알림 요청");
  });

  it("선착순 구간에서 신청 중이면 알림 요청 취소", () => {
    expect(attendButtonLabel("waiting", true, true)).toBe("알림 요청 취소");
  });

  it("선착순 구간이라도 자리가 있으면 참석하기 — 알림을 요청할 이유가 없다", () => {
    expect(attendButtonLabel("none", false, true)).toBe("참석하기");
  });

  it("선착순 구간에 대기 중인데 자리가 났으면 참석하기 — 직접 눌러야 한다", () => {
    expect(attendButtonLabel("waiting", false, true)).toBe("참석하기");
  });
});

describe("waitHintText — 대기 중에는 '내가 취소해야 한다'가 늘 보인다", () => {
  it("참석 중이면 줄을 그리지 않는다", () => {
    expect(waitHintText("attending", null, 3, false)).toBeNull();
  });

  it("대기 중이면 순번과 자동 확정을 함께 알린다", () => {
    expect(waitHintText("waiting", 3, 5, false)).toBe("대기 3번 · 자리 나면 자동 확정");
  });

  it("순번이 아직 안 왔으면(낙관적 반영 중) 숫자를 지어내지 않는다", () => {
    expect(waitHintText("waiting", null, 5, false)).toBe("대기 중");
  });

  it("미신청이고 대기자가 있으면 인원만 알린다", () => {
    expect(waitHintText("none", null, 2, false)).toBe("현재 2명 대기 중");
  });

  it("대기자가 없으면 줄을 그리지 않는다", () => {
    expect(waitHintText("none", null, 0, false)).toBeNull();
  });

  it("선착순 구간에서 신청 중이면 순번을 말하지 않는다 — 순번이 없다", () => {
    expect(waitHintText("waiting", 3, 5, true)).toBe("빈 자리가 나면 알려드려요");
  });

  it("선착순 구간에서 미신청이면 대기 인원을 말하지 않는다", () => {
    expect(waitHintText("none", null, 5, true)).toBeNull();
  });
});

describe("waitConfirmCopy — 누르기 전에 무슨 일이 일어나는지 알린다", () => {
  it("2시간 전까지: 자동 참석 사실 → 경고 순으로 네 줄", () => {
    const copy = waitConfirmCopy(false);
    expect(copy.title).toBe("대기 신청할까요?");
    expect(copy.confirmLabel).toBe("대기 신청");
    expect(copy.lines).toEqual([
      "참석자가 취소하면 순번대로 자동 참석됩니다.",
      "참석이 어려우면 미리 대기를 취소해주세요.",
      "시작 2시간 전부터는 대기 순번이 없고 선착순으로 바뀝니다.",
      "이후 빈 자리가 나면 알림을 보내드리니 직접 참석해주세요.",
    ]);
  });

  it("선착순 구간: 알림 요청이라는 것만 두 줄로", () => {
    const copy = waitConfirmCopy(true);
    expect(copy.title).toBe("빈 자리 알림을 요청할까요?");
    expect(copy.confirmLabel).toBe("알림 요청");
    expect(copy.lines).toEqual([
      "지금은 순번 없이 선착순입니다.",
      "빈 자리가 나면 알림을 보내드립니다.",
    ]);
  });

  it("경고는 둘째 줄이다 — 화면이 이 줄만 굵게 처리한다", () => {
    expect(waitConfirmCopy(false).lines[1]).toContain("미리 대기를 취소");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm vitest run lib/gathering/__tests__/waitlist.test.ts`
Expected: FAIL — `waitConfirmCopy is not a function` 및 라벨 불일치

- [ ] **Step 3: 구현**

`lib/gathering/waitlist.ts` 의 `attendButtonLabel`·`waitHintText` 를 교체하고 `waitConfirmCopy` 를 추가:

```ts
/**
 * 참석 버튼 라벨.
 *
 * 만석이어도 "인원 마감"으로 막지 않는다 — 그게 이 기능의 발단이다(더 오고 싶은
 * 사람에게 길이 없었다).
 *
 * `openToAll`(시작 2시간 전부터)이면 **"대기"라는 말을 쓰지 않는다.** 그 구간엔 순번도
 * 자동 확정도 없고 받는 것은 알림 하나뿐인데, "대기"라고 부르면 버튼이 지키지 못할
 * 약속을 한다(설계 §4-2).
 */
export function attendButtonLabel(
  state: AttendState,
  isFull: boolean,
  openToAll: boolean,
): string {
  if (state === "attending") return "✅ 참석";
  // 선착순 구간에 자리가 났으면 대기 중이던 사람도 직접 눌러야 한다 — 자동 승급이 없다.
  if (!isFull && (state === "none" || openToAll)) return "참석하기";
  if (state === "waiting") return openToAll ? "알림 요청 취소" : "대기 취소";
  return openToAll ? "빈 자리 알림 요청" : "대기 신청";
}

/**
 * 버튼 옆 보조 안내. 없으면 null(줄을 그리지 않는다).
 *
 * 대기 중인데 `rank`가 null인 경우가 있다 — 낙관적 업데이트로 "대기 중"까지만 즉시
 * 반영하고 순번은 서버 응답을 기다리는 찰나다. 숫자를 미리 지어내면 "3번이었는데
 * 5번이 됐다"로 보인다.
 *
 * **대기 중 문구에 "자리 나면 자동 확정"을 붙이는 것이 핵심이다.** 확인 다이얼로그는 한 번
 * 보고 지나가지만 이 줄은 줄 서 있는 내내 남아, "안 갈 거면 내가 빼야 한다"를 계속 말한다.
 */
export function waitHintText(
  state: AttendState,
  rank: number | null,
  total: number,
  openToAll: boolean,
): string | null {
  if (state === "attending") return null;
  if (openToAll) {
    // 순번이 없는 구간이라 숫자를 말하지 않는다. 미신청자에게는 할 말이 없다
    // (버튼이 이미 "빈 자리 알림 요청"이라고 적혀 있다).
    return state === "waiting" ? "빈 자리가 나면 알려드려요" : null;
  }
  if (state === "waiting") {
    return rank === null ? "대기 중" : `대기 ${rank}번 · 자리 나면 자동 확정`;
  }
  return total > 0 ? `현재 ${total}명 대기 중` : null;
}

/** 대기 신청 확인 다이얼로그 문구. `lines[1]`이 경고라 화면이 그 줄만 강조한다. */
export type WaitConfirmCopy = { title: string; lines: string[]; confirmLabel: string };

/**
 * 대기 신청 전 확인 문구.
 *
 * **누르기 전에** 알려야 한다 — 자동으로 참석자가 되는 건 되돌리기 번거로운 일이고
 * (임박 취소는 사유가 필수다) 사후 토스트로는 늦다.
 *
 * 취소 페널티는 여기 적지 않는다: 신청하는 자리에서 불이익까지 설명하면 겁주는 문서가
 * 된다. 필요한 건 "안 갈 거면 네가 빼라" 하나다.
 *
 * "2시간 뒤 대기가 취소된다"고도 쓰지 않는다 — 사실도 아니고(행을 지우지 않는다)
 * 박탈로 읽힌다. 실제로는 여전히 참석할 수 있고 순번만 무의미해진다.
 */
export function waitConfirmCopy(openToAll: boolean): WaitConfirmCopy {
  if (openToAll) {
    return {
      title: "빈 자리 알림을 요청할까요?",
      lines: ["지금은 순번 없이 선착순입니다.", "빈 자리가 나면 알림을 보내드립니다."],
      confirmLabel: "알림 요청",
    };
  }
  return {
    title: "대기 신청할까요?",
    lines: [
      "참석자가 취소하면 순번대로 자동 참석됩니다.",
      "참석이 어려우면 미리 대기를 취소해주세요.",
      "시작 2시간 전부터는 대기 순번이 없고 선착순으로 바뀝니다.",
      "이후 빈 자리가 나면 알림을 보내드리니 직접 참석해주세요.",
    ],
    confirmLabel: "대기 신청",
  };
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm vitest run lib/gathering/__tests__/waitlist.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/gathering/waitlist.ts lib/gathering/__tests__/waitlist.test.ts
git commit -m "feat(gathering): 선착순 구간 버튼 라벨·안내 문구 분기"
```

---

### Task 3: 마이그레이션 — 승급 2시간 게이트

**Files:**
- Create: `supabase/migrations/20260911100000_gthr_promote_openall_gate.sql`

**Interfaces:**
- Consumes: `promote_gthr_waitlist(p_gthr_id uuid, p_team_id uuid) RETURNS uuid[]` (기존)
- Produces: 같은 시그니처. 시작 2시간 미만이면 **항상 빈 배열**

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 락 대기만 짧게 끊는다(실행 자체는 끝까지). supabase/migrations/README.md 체크리스트.
SET lock_timeout = '3s';

-- ============================================================
-- 모임 대기열 — 시작 2시간 전부터 자동 승급 중단
--   설계: docs/superpowers/specs/2026-09-11-모임-대기열-임박구간-design.md §2
--
-- 왜: 모임이 코앞이면 "가장 오래 기다린 사람"보다 **지금 올 수 있는 사람**이 중요하다.
-- 대기 1번이 알림을 못 보면 자리는 빈 채로 모임이 시작된다. 그래서 이 구간엔 줄을
-- 당기지 않고 선착순으로 연다(앱이 대기자 전원에게 gthr_seat 알림을 보낸다).
--
-- 값(2시간)은 TS 쪽 GATHERING_WAITLIST_OPEN_HOURS 와 **같은 값을 각자 들고 있다.**
-- SQL 이 TS 상수를 읽을 수 없어 생기는 중복이라, 한쪽을 바꾸면 반드시 다른 쪽도 바꾼다.
-- ============================================================

CREATE OR REPLACE FUNCTION public.promote_gthr_waitlist(
  p_gthr_id uuid,
  p_team_id uuid
)
RETURNS uuid[]
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
  v_max      int;
  v_aprv     boolean;
  v_open     boolean;
  v_seatrace boolean;
  v_cnt      int;
  v_promoted uuid[] := ARRAY[]::uuid[];
  r          record;
BEGIN
  -- team_id 필터는 IDOR 방어(gthr_id 는 클라 입력에서 출발한다).
  SELECT max_prt_cnt,
         aprv_req_yn,
         COALESCE(end_at, stt_at) > now(),
         stt_at - now() < interval '2 hours'
    INTO v_max, v_aprv, v_open, v_seatrace
  FROM   public.gthr_mst
  WHERE  gthr_id = p_gthr_id AND team_id = p_team_id AND del_yn = false
  FOR UPDATE;
  IF NOT FOUND THEN RETURN v_promoted; END IF;

  -- 승인제는 운영진이 고른다(대기열을 붙이지 않는다). 방어적 재확인.
  IF v_aprv THEN RETURN v_promoted; END IF;
  -- 정원 없는 모임엔 대기열 개념이 없다.
  IF v_max IS NULL THEN RETURN v_promoted; END IF;
  -- 이미 시작·종료한 모임엔 시스템이 사람을 자동으로 밀어 넣지 않는다.
  IF NOT v_open THEN RETURN v_promoted; END IF;
  -- 시작 2시간 전부터는 선착순 구간 — 줄을 당기지 않는다.
  -- ⚠️ 반환값만 보면 "올릴 사람이 없었다"와 구분되지 않는다. 호출부(TS)가 stt_at 을
  --    이미 들고 있으므로 isWaitlistOpenToAll 로 스스로 판정해 알림을 보낸다.
  IF v_seatrace THEN RETURN v_promoted; END IF;

  SELECT COUNT(*) INTO v_cnt FROM public.gthr_attd_rel WHERE gthr_id = p_gthr_id;

  FOR r IN
    SELECT w.wait_id, w.mem_id
    FROM   public.gthr_wait_rel w
    WHERE  w.gthr_id = p_gthr_id AND w.wait_st_cd = 'waiting'
    ORDER  BY w.wait_at, w.wait_id
    FOR UPDATE
  LOOP
    EXIT WHEN v_cnt >= v_max;

    -- 이미 참석자다(운영진이 우겨넣었다) → 대기 행만 닫고 **자리는 쓰지 않은 채**
    -- 다음 사람을 본다. 여기서 v_cnt 를 올리면 빈자리가 하나 증발한다.
    IF EXISTS (
      SELECT 1 FROM public.gthr_attd_rel a
      WHERE  a.gthr_id = p_gthr_id AND a.mem_id = r.mem_id
    ) THEN
      UPDATE public.gthr_wait_rel
      SET    wait_st_cd = 'promoted', upd_at = now()
      WHERE  wait_id = r.wait_id;
      CONTINUE;
    END IF;

    -- 탈퇴·비활성은 건너뛴다. 대기 행은 waiting 그대로 둔다 — 재활성되면 다시 후보다.
    IF NOT EXISTS (
      SELECT 1 FROM public.team_mem_rel t
      WHERE  t.mem_id = r.mem_id AND t.team_id = p_team_id
        AND  t.vers = 0 AND t.del_yn = false AND t.mem_st_cd = 'active'
    ) THEN
      CONTINUE;
    END IF;

    -- 이 INSERT 가 "확정"의 정의다 — 포인트 트리거·집계가 여기서 비로소 돈다.
    INSERT INTO public.gthr_attd_rel (gthr_id, mem_id)
    VALUES (p_gthr_id, r.mem_id)
    ON CONFLICT (gthr_id, mem_id) DO NOTHING;

    UPDATE public.gthr_wait_rel
    SET    wait_st_cd = 'promoted', upd_at = now()
    WHERE  wait_id = r.wait_id;

    v_cnt      := v_cnt + 1;
    v_promoted := v_promoted || r.mem_id;
  END LOOP;

  RETURN v_promoted;
END;
$$;

COMMENT ON FUNCTION public.promote_gthr_waitlist(uuid, uuid)
  IS '비승인제 모임의 빈자리만큼 대기자를 wait_at 순으로 참석 확정시킨다. 시작 2시간 전부터는 선착순 구간이라 아무도 올리지 않는다. 승급된 mem_id 배열 반환. service_role 전용.';

REVOKE ALL ON FUNCTION public.promote_gthr_waitlist(uuid, uuid) FROM public, authenticated, anon;
GRANT EXECUTE ON FUNCTION public.promote_gthr_waitlist(uuid, uuid) TO service_role;

-- ============================================================
-- REVERT — 20260910110000_gthr_wait_rpcs.sql 의 promote_gthr_waitlist 본문으로 되돌린다
--          (v_seatrace 선언과 게이트만 제거하면 된다).
-- ============================================================
```

- [ ] **Step 2: dev DB에 적용**

MCP `mcp__ggd__apply_migration` 으로 name `gthr_promote_openall_gate`, 위 SQL 전문을 적용한다.

- [ ] **Step 3: dev DB에서 게이트 동작 확인**

`mcp__ggd__execute_sql` 로 함수 본문에 게이트가 들어갔는지 확인:

```sql
select pg_get_functiondef(oid) like '%interval ''2 hours''%' as has_gate
from pg_proc where proname = 'promote_gthr_waitlist';
```

Expected: `has_gate = true`

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260911100000_gthr_promote_openall_gate.sql
git commit -m "feat(gathering): 시작 2시간 전부터 대기열 자동 승급 중단"
```

---

### Task 4: 마이그레이션 — 알림 타입 `gthr_seat`

**Files:**
- Create: `supabase/migrations/20260911110000_noti_gthr_seat_type.sql`

**Interfaces:**
- Produces: `noti_mst.noti_type_enm` 에 `'gthr_seat'` 허용

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 락 대기만 짧게 끊는다(실행 자체는 끝까지). supabase/migrations/README.md 체크리스트.
SET lock_timeout = '3s';

-- ============================================================
-- 모임 대기열 — 알림 타입 gthr_seat (선착순 구간 빈 자리 발생)
--   설계: docs/superpowers/specs/2026-09-11-모임-대기열-임박구간-design.md §3
--
-- gthr_promo(자동 승급)와 **반드시 갈라 둔다.** 어휘가 다르기 때문이다 —
-- "확정됐어요"(자동, 할 일 없음) vs "빈 자리가 났어요"(직접 눌러야 함).
-- 같은 타입으로 묶으면 선착순 구간 알림을 받고 "확정됐구나" 하고 안 누르는 사람이 생긴다.
--
-- 딥링크 맵(lib/notifications/deep-link.ts)과 알림 라벨·아이콘도 함께 갱신해야 한다 —
-- 여기만 열고 맵을 빼먹으면 알림을 눌러도 아무 데도 안 간다.
-- ============================================================

ALTER TABLE public.noti_mst DROP CONSTRAINT IF EXISTS noti_mst_noti_type_enm_check;

ALTER TABLE public.noti_mst ADD CONSTRAINT noti_mst_noti_type_enm_check
  CHECK (noti_type_enm = ANY (ARRAY[
    'ttl_grnt'::text, 'adm_cust'::text, 'dues_check_req'::text, 'dues_notice'::text,
    'cmnt_reply'::text, 'cmnt_mention'::text, 'sch_post_cmnt'::text, 'sch_post_new'::text,
    'gthr_new'::text, 'gthr_upd'::text, 'gthr_del'::text, 'gthr_cmnt'::text,
    'gthr_reply'::text, 'gthr_mention'::text, 'gthr_cncl'::text, 'fdbk_new'::text,
    'fdbk_rspd'::text, 'newbie_nudge_14'::text, 'newbie_nudge_28'::text,
    'reactivate_req'::text,
    'post_cmnt'::text, 'post_reply'::text,
    'brd_notice'::text, 'brd_update'::text,
    -- 모임 참가 신청·승인·반려
    'gthr_aply'::text, 'gthr_aprv'::text, 'gthr_rjct'::text,
    -- 모임 대기열 승급
    'gthr_promo'::text,
    -- 모임 선착순 구간 빈 자리 발생
    'gthr_seat'::text
  ])) NOT VALID;

-- 기존 행 검증 — 행 락을 잡지 않는다(SHARE UPDATE EXCLUSIVE).
ALTER TABLE public.noti_mst VALIDATE CONSTRAINT noti_mst_noti_type_enm_check;

-- ============================================================
-- REVERT — 20260910120000_noti_gthr_promo_type.sql 의 배열(gthr_seat 제외)로 되돌린다.
-- ============================================================
```

- [ ] **Step 2: dev DB에 적용**

MCP `mcp__ggd__apply_migration` 으로 name `noti_gthr_seat_type`, 위 SQL 전문 적용.

- [ ] **Step 3: 확인**

`mcp__ggd__execute_sql`:

```sql
select pg_get_constraintdef(oid) like '%gthr_seat%' as ok
from pg_constraint where conname = 'noti_mst_noti_type_enm_check';
```

Expected: `ok = true`

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260911110000_noti_gthr_seat_type.sql
git commit -m "feat(noti): 빈 자리 알림 타입 gthr_seat 추가"
```

---

### Task 5: 빈 자리 알림 발송 코어

**Files:**
- Create: `lib/gathering/seat-notice.ts`
- Test: `lib/gathering/__tests__/seat-notice.test.ts`

**Interfaces:**
- Consumes: `insertNotiMany` (`@/lib/notifications/insert-noti`), untyped admin client
- Produces: `notifyOpenSeat(admin, { gthrId, gthrNm, teamId }): Promise<string[]>` — 알림을 보낸 mem_id 배열

> 두 호출부(본인 취소·운영진 제거)가 공유한다. 각자 만들면 한쪽만 중복 방지를 빠뜨린다.

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/gathering/__tests__/seat-notice.test.ts`:

```ts
import { beforeEach, describe, expect, it, vi } from "vitest";

const h = vi.hoisted(() => ({
  insertNotiMany: vi.fn(async () => ({ inAppOk: true, notifiedMemIds: [] as string[] })),
  waiting: { data: [] as { mem_id: string }[] },
  alreadyNotified: { data: [] as { mem_id: string }[] },
}));

vi.mock("@/lib/notifications/insert-noti", () => ({ insertNotiMany: h.insertNotiMany }));

// from("gthr_wait_rel") → waiting, from("noti_mst") → alreadyNotified 를 돌려주는 최소 스텁.
const admin = {
  from(table: string) {
    const result = table === "gthr_wait_rel" ? h.waiting : h.alreadyNotified;
    const p = Promise.resolve(result);
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const proxy: any = new Proxy(function () {}, {
      get(_t, prop) {
        if (prop === "then") return p.then.bind(p);
        if (prop === "catch") return p.catch.bind(p);
        return () => proxy;
      },
      apply: () => proxy,
    });
    return proxy;
  },
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
} as any;

import { notifyOpenSeat } from "@/lib/gathering/seat-notice";

describe("notifyOpenSeat", () => {
  beforeEach(() => {
    vi.clearAllMocks();
    h.waiting.data = [];
    h.alreadyNotified.data = [];
  });

  it("대기자 전원에게 보낸다", async () => {
    h.waiting.data = [{ mem_id: "m1" }, { mem_id: "m2" }];
    const sent = await notifyOpenSeat(admin, {
      gthrId: "g1",
      gthrNm: "수요 한강런",
      teamId: "t1",
    });
    expect(sent).toEqual(["m1", "m2"]);
    expect(h.insertNotiMany).toHaveBeenCalledWith(
      expect.objectContaining({
        memIds: ["m1", "m2"],
        notiTypeEnm: "gthr_seat",
        notiNm: "'수요 한강런' 빈 자리가 났어요",
        refId: "g1",
      }),
    );
  });

  it("이미 받은 사람은 제외한다 — 당일 취소가 서너 건이라 도배된다", async () => {
    h.waiting.data = [{ mem_id: "m1" }, { mem_id: "m2" }];
    h.alreadyNotified.data = [{ mem_id: "m1" }];
    const sent = await notifyOpenSeat(admin, {
      gthrId: "g1",
      gthrNm: "수요 한강런",
      teamId: "t1",
    });
    expect(sent).toEqual(["m2"]);
  });

  it("보낼 사람이 없으면 알림을 부르지 않는다", async () => {
    h.waiting.data = [{ mem_id: "m1" }];
    h.alreadyNotified.data = [{ mem_id: "m1" }];
    const sent = await notifyOpenSeat(admin, {
      gthrId: "g1",
      gthrNm: "수요 한강런",
      teamId: "t1",
    });
    expect(sent).toEqual([]);
    expect(h.insertNotiMany).not.toHaveBeenCalled();
  });

  it("대기자가 없으면 조용히 끝난다", async () => {
    const sent = await notifyOpenSeat(admin, {
      gthrId: "g1",
      gthrNm: "수요 한강런",
      teamId: "t1",
    });
    expect(sent).toEqual([]);
    expect(h.insertNotiMany).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm vitest run lib/gathering/__tests__/seat-notice.test.ts`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

`lib/gathering/seat-notice.ts`:

```ts
import "server-only";

import { insertNotiMany } from "@/lib/notifications/insert-noti";
import type { createUntypedAdminClient } from "@/lib/supabase/admin";

type SupabaseLike = ReturnType<typeof createUntypedAdminClient>;

/** 알림 제목·본문의 정본. 화면·테스트가 같은 문자열을 본다. */
export const SEAT_NOTI_TITLE = (gthrNm: string) => `'${gthrNm}' 빈 자리가 났어요`;
export const SEAT_NOTI_BODY = "지금은 순번 없이 먼저 누르는 분이 참석하실 수 있어요.";

/**
 * 선착순 구간(시작 2시간 전부터)에 빈 자리가 났음을 대기자 전원에게 알린다.
 * 설계: docs/superpowers/specs/2026-09-11-모임-대기열-임박구간-design.md §3
 *
 * **이 알림이 없으면 지금보다 나빠진다.** 그 구간엔 자동 승급이 없으므로, 알림까지 없으면
 * 대기자 전원이 모른 채 자리가 빈 채로 모임이 시작된다.
 *
 * **한 모임당 1회로 제한한다.** 정기런은 당일 취소가 서너 건씩 나는데 매번 보내면
 * 알림함이 도배된다 — 이미 받은 사람에게 "자리가 하나 더 났다"는 새 소식이 아니다
 * (유리해졌을 뿐 할 일이 같다).
 *
 * **시각을 약속하지 않는다.** 알림은 한 번 나가면 고쳐지지 않는데, "몇 시부터 가능"을
 * 적으면 그 사이 자리가 차서 거짓말이 된다. 지금 사실만 말하고 나머지는 화면이 맡는다.
 *
 * 두 호출부(본인 취소·운영진 제거)가 공유한다 — 각자 만들면 한쪽만 중복 방지를 빠뜨린다.
 *
 * @returns 실제로 알림을 보낸 mem_id 배열(호출부 로깅용)
 */
export async function notifyOpenSeat(
  admin: SupabaseLike,
  { gthrId, gthrNm, teamId }: { gthrId: string; gthrNm: string; teamId: string },
): Promise<string[]> {
  const { data: waitRows } = await admin
    .from("gthr_wait_rel")
    .select("mem_id")
    .eq("gthr_id", gthrId)
    .eq("wait_st_cd", "waiting");
  const waiting = ((waitRows ?? []) as { mem_id: string }[]).map((w) => w.mem_id);
  if (waiting.length === 0) return [];

  // 이 모임에 대해 이미 gthr_seat 를 받은 사람 제외(1회 제한).
  const { data: sentRows } = await admin
    .from("noti_mst")
    .select("mem_id")
    .eq("ref_id", gthrId)
    .eq("noti_type_enm", "gthr_seat")
    .in("mem_id", waiting);
  const already = new Set(((sentRows ?? []) as { mem_id: string }[]).map((r) => r.mem_id));

  const targets = waiting.filter((memId) => !already.has(memId));
  if (targets.length === 0) return [];

  await insertNotiMany({
    teamId,
    memIds: targets,
    notiTypeEnm: "gthr_seat",
    notiNm: SEAT_NOTI_TITLE(gthrNm),
    notiCont: SEAT_NOTI_BODY,
    refId: gthrId,
    refTypeEnm: "gathering",
  });

  return targets;
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm vitest run lib/gathering/__tests__/seat-notice.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/gathering/seat-notice.ts lib/gathering/__tests__/seat-notice.test.ts
git commit -m "feat(gathering): 빈 자리 알림 발송 코어(1회 제한)"
```

---

### Task 6: 취소 경로 두 곳에서 빈 자리 알림 발송

**Files:**
- Modify: `app/actions/gathering/toggle-attendance.ts`
- Modify: `app/actions/admin/manage-gathering-attendance.ts`
- Test: `app/actions/__tests__/gathering-waitlist.test.ts`

**Interfaces:**
- Consumes: `notifyOpenSeat` (Task 5), `isWaitlistOpenToAll` (Task 1)

- [ ] **Step 1: 실패하는 테스트 작성**

`app/actions/__tests__/gathering-waitlist.test.ts` 에 추가한다. 기존 `h` 스텁에 `notifyOpenSeat` 모킹을 더한다:

```ts
// h = vi.hoisted(...) 안에 추가
const notifyOpenSeat = vi.fn(async () => ["m2", "m3"]);

// 파일 상단 vi.mock 목록에 추가
vi.mock("@/lib/gathering/seat-notice", () => ({ notifyOpenSeat: h.notifyOpenSeat }));
```

그리고 describe 블록 추가:

```ts
describe("임박 구간 취소 — 승급 대신 빈 자리 알림", () => {
  it("시작 1시간 전 취소는 대기자에게 알림을 보낸다", async () => {
    // 참석 중인 상태에서 취소
    h.cfg.existing.data = { attd_id: "a1" };
    h.cfg.gthr.data.stt_at = dayjs().add(1, "hour").toISOString();
    h.rpc.mockResolvedValue({ data: [], error: null }); // 승급 없음(RPC 게이트)

    await toggleGatheringAttendance("g1", "개인 사정");

    expect(h.notifyOpenSeat).toHaveBeenCalledWith(
      expect.anything(),
      expect.objectContaining({ gthrId: "g1" }),
    );
  });

  it("시작 5시간 전 취소는 알림을 보내지 않는다 — 자동 승급이 도는 구간이다", async () => {
    h.cfg.existing.data = { attd_id: "a1" };
    h.cfg.gthr.data.stt_at = dayjs().add(5, "hour").toISOString();
    h.rpc.mockResolvedValue({ data: ["m2"], error: null });

    await toggleGatheringAttendance("g1");

    expect(h.notifyOpenSeat).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm vitest run app/actions/__tests__/gathering-waitlist.test.ts`
Expected: FAIL — `notifyOpenSeat` 미호출

- [ ] **Step 3: 구현 — toggle-attendance.ts**

import 추가:

```ts
import { isWaitlistOpenToAll } from "@/lib/gathering/cancel-imminent";
import { notifyOpenSeat } from "@/lib/gathering/seat-notice";
```

취소 분기의 `after(async () => { await Promise.all([ ... ]) })` 배열 안, 승급자 알림 `...promoted.map(...)` **바로 뒤**에 추가:

```ts
          // 선착순 구간(시작 2시간 전~)이면 RPC 가 아무도 안 올린다 — 대신 대기자
          // 전원에게 "빈 자리가 났어요"를 보낸다. 이 알림이 없으면 자동 승급도 알림도
          // 없어 자리가 빈 채로 모임이 시작된다(설계 §3).
          //
          // promoted.length === 0 을 조건에 쓰지 않는다: 빈 배열은 "올릴 사람이 없었다"
          // 와도 같은 값이라, 시각으로 판정하는 쪽이 뜻이 분명하다.
          isWaitlistOpenToAll(gthr.stt_at)
            ? notifyOpenSeat(admin, {
                gthrId: gthr_id,
                gthrNm: gthr.gthr_nm,
                teamId,
              }).catch((e) => console.error("[gthr_seat] 빈 자리 알림 실패", e))
            : Promise.resolve(),
```

- [ ] **Step 4: 구현 — manage-gathering-attendance.ts**

`removeGatheringAttendance` 의 승급 알림 블록을 아래로 바꾼다. 모임 조회에 `stt_at` 을 더한다:

```ts
    const { data: gthrRow } = await db
      .from("gthr_mst")
      .select("gthr_nm, stt_at")
      .eq("gthr_id", gthrId)
      .maybeSingle();
    const gthrNm = gthrRow?.gthr_nm ?? "모임";

    const promoted: string[] = Array.isArray(promotedRaw) ? promotedRaw : [];
    after(async () => {
      await Promise.all([
        // 승급자에게 — 운영진이 뺀 것도 자리가 나는 사건이다.
        ...promoted.map((promotedMemId) =>
          insertNoti({
            teamId,
            memId: promotedMemId,
            notiTypeEnm: "gthr_promo",
            notiNm: `'${gthrNm}' 자리가 나서 참석이 확정됐어요`,
            notiCont: "대기 중이던 모임에 자리가 생겨 자동으로 참석 처리했어요.",
            refId: gthrId,
            refTypeEnm: "gathering",
          }).catch((e) => console.error("[gthr_promo] 알림 발송 실패", e)),
        ),
        // 선착순 구간이면 승급 대신 대기자 전원에게 빈 자리 알림(본인 취소 경로와 같다).
        gthrRow?.stt_at && isWaitlistOpenToAll(gthrRow.stt_at)
          ? notifyOpenSeat(untyped, { gthrId, gthrNm, teamId }).catch((e) =>
              console.error("[gthr_seat] 빈 자리 알림 실패", e),
            )
          : Promise.resolve(),
      ]);
    });
```

import 추가:

```ts
import { isWaitlistOpenToAll } from "@/lib/gathering/cancel-imminent";
import { notifyOpenSeat } from "@/lib/gathering/seat-notice";
```

- [ ] **Step 5: 통과 확인**

Run: `pnpm vitest run app/actions/__tests__/`
Expected: PASS (기존 취소 테스트 3종 포함 전부)

- [ ] **Step 6: 커밋**

```bash
git add app/actions/gathering/toggle-attendance.ts app/actions/admin/manage-gathering-attendance.ts app/actions/__tests__/gathering-waitlist.test.ts
git commit -m "feat(gathering): 선착순 구간 취소 시 대기자에게 빈 자리 알림"
```

---

### Task 7: 알림 딥링크·라벨·아이콘

**Files:**
- Modify: `lib/notifications/deep-link.ts`
- Modify: `components/notifications/notification-bell-icon.tsx:44` 부근
- Modify: `components/notifications/notification-item.tsx:33` 부근

**Interfaces:**
- Consumes: Task 4 의 `gthr_seat` 타입

- [ ] **Step 1: 딥링크 추가**

`lib/notifications/deep-link.ts` 의 `gthr_promo` 줄 **바로 아래**:

```ts
  // 선착순 구간 빈 자리 — 그 모임으로. 거기서 직접 "참석하기"를 눌러야 한다.
  gthr_seat: (refId) => (refId ? `${SCHEDULE}?gthr=${refId}` : SCHEDULE),
```

- [ ] **Step 2: 라벨 추가**

`components/notifications/notification-bell-icon.tsx` 의 `gthr_promo: "대기 승급",` 아래:

```ts
  gthr_seat: "빈 자리",
```

- [ ] **Step 3: 아이콘 추가**

`components/notifications/notification-item.tsx` 의 `gthr_promo: Users,` 아래:

```ts
  gthr_seat: Users,
```

- [ ] **Step 4: 타입 체크**

Run: `pnpm exec tsc --noEmit`
Expected: Task 2 에서 인자가 늘어난 `attendButtonLabel`·`waitHintText` 호출부 오류만 남는다(Task 8·9에서 해소). 그 외 오류 0건.

- [ ] **Step 5: 커밋**

```bash
git add lib/notifications/deep-link.ts components/notifications/
git commit -m "feat(noti): gthr_seat 딥링크·라벨·아이콘"
```

---

### Task 8: 다이얼로그에 대기 명단 + 내 순번

**Files:**
- Modify: `components/schedule/gathering-detail-dialog.tsx`
- Test: 없음 (클라이언트 조회 + 렌더. 수동 확인)

**Interfaces:**
- Consumes: `createClient` (`@/lib/supabase/client`), `GatheringWaitlist`·`WaitlistMember` (`@/components/schedule/gathering-waitlist`), `sortWaitlist`·`waitRankOf`·`isWaitlistOpenToAll`
- Produces: 다이얼로그가 `waitlist` state 를 갖고 `state`/`waitRank`/`waitCount` 초기값을 실제 값으로 세운다

- [ ] **Step 1: 대기 명단 조회 추가**

import 추가:

```ts
import { GatheringWaitlist, type WaitlistMember } from "@/components/schedule/gathering-waitlist";
import { isWaitlistOpenToAll } from "@/lib/gathering/cancel-imminent";
import { waitRankOf } from "@/lib/gathering/waitlist";
import { createClient } from "@/lib/supabase/client";
```

state 추가 (`const [waitCount, setWaitCount] = useState(0);` 아래):

```ts
  const [waitlist, setWaitlist] = useState<WaitlistMember[]>([]);
```

조회 콜백 추가 (`loadMyApplication` 정의 아래):

```ts
  /**
   * 대기 명단 — **RPC 가 아니라 테이블을 직접 조회한다.**
   *
   * get_gathering_detail 은 anon 실행이 허용된 SECURITY DEFINER 라 대기자 이름을 실으면
   * 비로그인에게 샌다. 반면 gthr_wait_rel 에는 `authenticated` + 팀 멤버 한정 SELECT RLS 가
   * 걸려 있어, 세션 클라이언트로 직접 읽으면 RLS 가 알아서 막는다 — 비로그인 0행.
   * 그래서 RPC 수정도 마이그레이션도 없다(설계 §4-1).
   */
  const loadWaitlist = useCallback(async (gthrId: string) => {
    const supabase = createClient();
    const { data } = await supabase
      .from("gthr_wait_rel")
      .select("mem_id, wait_at, mem_mst(mem_nm, avatar_url)")
      .eq("gthr_id", gthrId)
      .eq("wait_st_cd", "waiting");

    const rows: WaitlistMember[] = (data ?? []).map((w) => {
      const mem = Array.isArray(w.mem_mst) ? w.mem_mst[0] : w.mem_mst;
      return {
        mem_id: w.mem_id,
        wait_at: w.wait_at,
        mem_nm: mem?.mem_nm ?? null,
        avatar_url: mem?.avatar_url ?? null,
      };
    });
    setWaitlist(rows);
  }, []);
```

- [ ] **Step 2: 열릴 때 조회하고 상태에 반영**

`useEffect(() => { if (!open) return; loadMyApplication(); }, [open, loadMyApplication]);` 를 아래로 교체:

```ts
  // 열릴 때마다 새로 받는다 — 다른 기기·운영진이 그 사이 승인했을 수 있다.
  useEffect(() => {
    if (!open) return;
    loadMyApplication();
    if (gathering?.id) void loadWaitlist(gathering.id);
  }, [open, gathering?.id, loadMyApplication, loadWaitlist]);

  // 대기 명단이 도착하면 내 상태를 맞춘다. 참석이 대기를 이긴다(attendStateOf).
  // ⚠️ 이 동기화 전에는 이미 대기 중인 사람에게도 버튼이 "참석하기"로 보인다 —
  //    선행 설계 §9-1 의 알려진 제약이 여기서 닫힌다.
  const myWaitRank = currentMemberId ? waitRankOf(waitlist, currentMemberId) : null;
```

`syncKey` 블록의 대기 초기화 3줄을 아래로 교체:

```ts
    setState(attendStateOf({ attending: initialIsAttending ?? false, waiting: false }));
    setWaitRank(null);
    setWaitCount(0);
    setWaitlist([]); // 다른 모임을 열었을 때 이전 명단이 잠깐 남는 것 방지
```

그리고 명단 도착 후 반영을 별도 파생 동기화로 둔다(`syncKey` 블록 **아래**):

```ts
  // 명단이 도착하면 내 대기 상태·순번·인원을 한 번 맞춘다. 토글로 바뀐 뒤에는
  // 액션 응답이 정본이므로, 명단 자체가 바뀔 때만(waitKey) 다시 맞춘다.
  const waitKey = `${gKey}:${waitlist.map((w) => w.mem_id).join(",")}`;
  const [lastWaitKey, setLastWaitKey] = useState(waitKey);
  if (waitKey !== lastWaitKey) {
    setLastWaitKey(waitKey);
    setWaitCount(waitlist.length);
    setWaitRank(myWaitRank);
    if (!initialIsAttending && myWaitRank !== null) setState("waiting");
  }
```

- [ ] **Step 3: 명단 렌더**

참석자 목록(`attendees.map(...)`)을 그리는 블록 **바로 뒤**, `<GatheringCanceledAttendees ... />` **앞**에 추가:

```tsx
            {/* 대기 명단 — 상세 페이지와 **같은 컴포넌트**를 쓴다(순번 표기가 한쪽만
                바뀌는 사고 방지). 비어 있으면 스스로 아무것도 그리지 않는다. */}
            <GatheringWaitlist entries={waitlist} />
```

- [ ] **Step 4: 버튼에 구간 전달**

이 다이얼로그가 `attendButtonLabel`·`waitHintText` 를 직접 부르는 자리에 세 번째/네 번째 인자를 넘긴다:

```ts
  const openToAll = gathering?.sttAt ? isWaitlistOpenToAll(gathering.sttAt) : false;
```

> 모임 시작 시각 prop 이름은 이 파일의 기존 표기를 따른다(`gathering.sttAt` 또는
> `gathering.stt_at`). 파일을 열어 실제 이름을 확인하고 맞춘다.

- [ ] **Step 5: 타입 체크 + 빌드**

Run: `pnpm exec tsc --noEmit`
Expected: 이 파일 관련 오류 0건

- [ ] **Step 6: 커밋**

```bash
git add components/schedule/gathering-detail-dialog.tsx
git commit -m "fix(gathering): 일정탭 다이얼로그에 대기 명단·내 순번 노출"
```

---

### Task 9: 대기 신청 확인 다이얼로그

**Files:**
- Create: `components/schedule/wait-confirm-dialog.tsx`
- Modify: `app/(info)/gatherings/[id]/gathering-attend-button.tsx`
- Modify: `components/schedule/gathering-detail-dialog.tsx`
- Test: `components/schedule/__tests__/wait-confirm-dialog-render.test.ts`

**Interfaces:**
- Consumes: `waitConfirmCopy` (Task 2)
- Produces: `<WaitConfirmDialog open onOpenChange openToAll onConfirm />`

- [ ] **Step 1: 실패하는 렌더 테스트 작성**

`components/schedule/__tests__/wait-confirm-dialog-render.test.ts`:

```ts
import { renderToStaticMarkup } from "react-dom/server";
import { createElement } from "react";
import { describe, expect, it } from "vitest";

import { WaitConfirmBody } from "@/components/schedule/wait-confirm-dialog";

const html = (openToAll: boolean) =>
  renderToStaticMarkup(createElement(WaitConfirmBody, { openToAll }));

describe("WaitConfirmBody", () => {
  it("2시간 전까지는 자동 참석과 취소 책임을 알린다", () => {
    const out = html(false);
    expect(out).toContain("참석자가 취소하면 순번대로 자동 참석됩니다.");
    expect(out).toContain("참석이 어려우면 미리 대기를 취소해주세요.");
    expect(out).toContain("시작 2시간 전부터는 대기 순번이 없고 선착순으로 바뀝니다.");
  });

  it("경고 줄만 굵게 — 네 줄이 같은 무게면 아무것도 안 읽힌다", () => {
    const out = html(false);
    expect(out).toMatch(/font-semibold[^>]*>참석이 어려우면 미리 대기를 취소해주세요\./);
  });

  it("선착순 구간에서는 '대기'라는 말을 쓰지 않는다", () => {
    const out = html(true);
    expect(out).toContain("지금은 순번 없이 선착순입니다.");
    expect(out).not.toContain("대기");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm vitest run components/schedule/__tests__/wait-confirm-dialog-render.test.ts`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

`components/schedule/wait-confirm-dialog.tsx`:

```tsx
"use client";

import { Body, Caption } from "@/components/common/typography";
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogContent,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";

import { waitConfirmCopy } from "@/lib/gathering/waitlist";
import { cn } from "@/lib/utils";

/**
 * 본문만 떼어 둔다 — 다이얼로그 껍데기(Radix 포털)는 renderToStaticMarkup 으로 못 찍는데,
 * 정작 회귀 테스트로 못박아야 하는 건 **문구와 강조 위치**다.
 */
export function WaitConfirmBody({ openToAll }: { openToAll: boolean }) {
  const { lines } = waitConfirmCopy(openToAll);
  return (
    <ul className="flex flex-col gap-2">
      {lines.map((line, i) => (
        <li key={line} className="flex gap-2">
          <Caption className="shrink-0">·</Caption>
          {/* 둘째 줄이 경고다(waitConfirmCopy). 네 줄이 같은 무게면 아무것도 안 읽힌다. */}
          <Caption className={cn("text-foreground", i === 1 && "font-semibold")}>{line}</Caption>
        </li>
      ))}
    </ul>
  );
}

/**
 * 대기 신청 확인 — **누르기 전에** 무슨 일이 일어나는지 알린다.
 *
 * 자동으로 참석자가 되는 건 되돌리기 번거로운 일이라(임박 취소는 사유가 필수다)
 * 사후 토스트로는 늦다. 설계 §4-2.
 */
export function WaitConfirmDialog({
  open,
  onOpenChange,
  openToAll,
  onConfirm,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  openToAll: boolean;
  onConfirm: () => void;
}) {
  const { title, confirmLabel } = waitConfirmCopy(openToAll);
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>
            <Body className="font-semibold">{title}</Body>
          </DialogTitle>
        </DialogHeader>

        <WaitConfirmBody openToAll={openToAll} />

        <DialogFooter>
          <Button variant="outline" onClick={() => onOpenChange(false)}>
            취소
          </Button>
          <Button
            onClick={() => {
              onOpenChange(false);
              onConfirm();
            }}
          >
            {confirmLabel}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm vitest run components/schedule/__tests__/wait-confirm-dialog-render.test.ts`
Expected: PASS

- [ ] **Step 5: 버튼에 연결 — gathering-attend-button.tsx**

import 추가:

```ts
import { WaitConfirmDialog } from "@/components/schedule/wait-confirm-dialog";
import { isWaitlistOpenToAll } from "@/lib/gathering/cancel-imminent";
```

state 추가:

```ts
  const [waitConfirmOpen, setWaitConfirmOpen] = useState(false);
  const openToAll = isWaitlistOpenToAll(sttAt);
```

`handleClick` 의 `handleJoin()` 호출 직전에 분기를 넣는다:

```ts
    // 만석이라 대기(또는 알림 요청)로 들어가는 경우에만 확인을 받는다.
    // 자리가 있어 바로 참석하는 경로는 그대로 1탭이다 — 확인할 것이 없다.
    if (isFull) {
      setWaitConfirmOpen(true);
      return;
    }
    handleJoin();
```

`attendButtonLabel`·`waitHintText` 호출에 `openToAll` 을 넘기고, JSX 하단(`<GatheringCancelDialog ... />` 옆)에 추가:

```tsx
      <WaitConfirmDialog
        open={waitConfirmOpen}
        onOpenChange={setWaitConfirmOpen}
        openToAll={openToAll}
        onConfirm={handleJoin}
      />
```

- [ ] **Step 6: 다이얼로그에도 같은 연결 — gathering-detail-dialog.tsx**

같은 방식으로 `WaitConfirmDialog` 를 붙인다. **두 화면이 같은 컴포넌트를 쓴다** — 각자 만들면 문구가 한쪽만 바뀐다.

- [ ] **Step 7: 전체 테스트 + 빌드**

```bash
pnpm vitest run
pnpm exec tsc --noEmit
pnpm run lint
pnpm run build
```

Expected: 전부 통과, 타입·린트 0건

- [ ] **Step 8: 커밋**

```bash
git add components/schedule/wait-confirm-dialog.tsx components/schedule/__tests__/ "app/(info)/gatherings/[id]/gathering-attend-button.tsx" components/schedule/gathering-detail-dialog.tsx
git commit -m "feat(gathering): 대기 신청 전 확인 다이얼로그"
```

---

### Task 10: DB 타입 재생성 + 임시 우회 정리

**Files:**
- Modify: `lib/supabase/database.types.ts`
- Modify: `app/actions/admin/manage-gathering-attendance.ts` (`listGatheringWaitlist` 의 untyped 우회)

**Interfaces:**
- Consumes: `gthr_wait_rel` 테이블 (dev·prd 양쪽 적용 완료)

> `gthr_wait_rel` 이 타입에 없어서 여러 곳이 `createUntypedAdminClient` + `any` 로 우회하고
> 있었다. Task 8 의 클라이언트 조회도 타입이 없으면 `any` 가 된다.

- [ ] **Step 1: 타입 재생성**

MCP `mcp__ggd__generate_typescript_types` 로 받아 `lib/supabase/database.types.ts` 에 반영한다.

- [ ] **Step 2: gthr_wait_rel 이 들어갔는지 확인**

Run: `grep -c "gthr_wait_rel" lib/supabase/database.types.ts`
Expected: 1 이상

- [ ] **Step 3: `listGatheringWaitlist` 의 `any` 제거**

`app/actions/admin/manage-gathering-attendance.ts` 의 `listGatheringWaitlist` 에서
`createUntypedAdminClient()` → `db`(typed) 로 바꾸고 `// eslint-disable-next-line @typescript-eslint/no-explicit-any` 와 `(row: any)` 를 걷어낸다. 주석의 "gen types 후 교체 예정"도 지운다.

- [ ] **Step 4: 전체 검증**

```bash
pnpm exec tsc --noEmit
pnpm vitest run
pnpm run lint
pnpm run build
```

Expected: 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add lib/supabase/database.types.ts app/actions/admin/manage-gathering-attendance.ts
git commit -m "chore(supabase): gthr_wait_rel 타입 생성 및 untyped 우회 제거"
```

---

## 완료 후 (사람이 할 일)

- [ ] **prd 마이그레이션 적용** — Task 3·4 의 SQL 두 개를 `mcp__ggp__apply_migration` 으로. **코드 배포 전에 먼저 적용한다**(순서가 뒤집히면 `gthr_seat` INSERT 가 CHECK 제약에 걸린다).
- [ ] **브라우저 확인** — 다이얼로그 대기 명단·순번, 확인 다이얼로그 두 문구, 2시간 경계 전후 버튼 라벨. (기존 대기 데이터가 dev 에 없으면 `gthr_wait_rel` 에 직접 INSERT 해 확인)
- [ ] PR 생성 (사용자 요청 시)
