# 마일리지런 프로젝트 설계서

> 기강 러닝크루 앱 — "프로젝트" 탭 첫 번째 프로젝트
> 최종 업데이트: 2026-03-27

---

## 1. 개요

기강 러닝크루 앱에 **프로젝트 탭**을 신규 추가하고, 첫 번째 프로젝트로 **마일리지런**을 운영한다.

- **프로젝트 탭**: 하단 탭바 가운데(3번째)에 위치. 항상 활성 프로젝트 1개가 바로 표시됨.
- **마일리지런**: 5월~9월(5개월) 동안 크루원 각자 목표 마일리지를 설정하고, 매월 달성하며 점진적으로 목표를 높여가는 이벤트.
- **연습 기간**: 4월(시작월 1달 전)은 연습 기간. 기록 입력/조회 가능하나 환급/회식비 계산 없음.
- 이 탭은 범용이며, 시즌에 따라 다른 프로젝트(예: 동계훈련)로 교체될 수 있음.

### 기술 스택

Next.js (App Router) + Supabase + Recharts + shadcn/ui + pnpm. 기존 패턴을 따를 것.

### 하단 탭바 순서

```
홈 → 대회 → 프로젝트 → 랭킹 → 프로필
```

---

## 2. 메인 화면 구성

### 2.1 레이아웃 (위→아래)

```
┌─────────────────────────────────┐
│ [프로젝트명]          [< 5월 >] │  ← 제목 + 월 네비게이터 (같은 줄)
├─────────────────────────────────┤
│ [참가 신청]                     │  ← 미참여자만 표시
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ 크루 진행현황 그래프        │ │  ← 마일리지/달성률 토글
│ │ N월 진행현황 통계           │ │  ← 같은 border 블록 안
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 내 현황                         │  ← 참여자만
├─────────────────────────────────┤
│ 환급 / 회식비                   │  ← 참여자만, 연습 기간이면 안내만
├─────────────────────────────────┤
│ 내 기록 (최신 5개 + 더보기)     │  ← 참여자만, 선택된 월 기록만
├─────────────────────────────────┤
│                          [+ FAB]│  ← 기록 입력 버튼
└─────────────────────────────────┘
```

### 2.2 접근 권한별 표시

| 상태 | 그래프+진행현황 | 참가신청 | 내현황/환급/기록 |
|------|:---:|:---:|:---:|
| 비로그인 | O | X | X |
| 로그인, 미참여 | O | O | X |
| 로그인, 신청 후 미승인 | O | O (대기 안내) | X |
| 로그인, 참여자 (승인됨) | O | X | O |

### 2.3 월 네비게이션

- **위치**: 제목 우측
- **형태**: `< 5월 >` 버튼
- **범위**: 시작월 1달 전(연습 기간) ~ 종료월
  - 예: 프로젝트 5월~9월 → 4월(연습)~9월까지 탐색 가능
- **구현**: `searchParams.month`로 서버 컴포넌트 재렌더
- 모든 하위 컴포넌트(그래프, 진행현황, 내현황, 기록 등)에 선택된 월을 전달

---

## 3. 연습 기간

- **정의**: 프로젝트 시작월 1달 전 (예: 5월 시작 → 4월이 연습)
- **연습 기간 특징**:
  - 기록 입력/조회 가능
  - 목표는 `initial_goal`과 동일
  - 환급/회식비 계산 **없음** → "N월은 연습 기간입니다" 안내 표시
  - 진행현황에 "(연습)" 라벨 표시
  - 환급 예상 총액, 회식비 풀 숨김

---

## 4. 마일리지 계산 규칙

### 4.1 종목별 기본 마일리지

| 종목 | 계산식 |
|------|--------|
| 러닝 | 거리(km) × 1 + 상승고도(m) / 100 |
| 트레일러닝 | 거리(km) × 1 + 상승고도(m) / 100 |
| 자전거 | 거리(km) / 4 + 상승고도(m) / 100 |
| 수영 | 거리(km) × 3 (상승고도 없음) |

- 거리 단위: km
- 상승고도 단위: m (수영 제외 전 종목 입력 가능)

### 4.2 이벤트 배율

운영진이 관리자 페이지(`/admin/events`)에서 추가/수정/삭제할 수 있는 배율 이벤트:

| 이벤트 예시 | 배율 |
|------------|------|
| 비 올 때 | ×1.2 |
| 모임에서 뛰기 | ×1.1 |

- 이벤트별 시작일/종료일 설정 가능
- 이벤트는 **중복 적용** 가능 (곱셈)
- 기록 입력 시 현재 활성 상태인 이벤트를 체크박스로 선택
- 이벤트가 비활성화되어도 기존 기록의 마일리지는 소급 변경하지 않음
- 이벤트 배율이 변경되어도 기존 기록에는 영향 없음 (입력 시점의 배율 스냅샷 저장)

### 4.3 최종 마일리지 계산

```
최종 마일리지 = 기본 마일리지 × 이벤트배율1 × 이벤트배율2 × ...
```

### 4.4 소수점 처리

- **저장**: 소수점 둘째자리까지 (`Math.round(x * 100) / 100`)
- **표시**: 소수점 첫째자리까지

---

## 5. 목표 및 월별 진행 규칙

### 5.1 초기 목표 설정

| 옵션 | 값 |
|------|-----|
| 초보 | 50 km/월 |
| 고수 | 100 km/월 |
| 초고수 | 자유 입력 (1 이상) |

### 5.2 월별 목표 자동 갱신

목표는 정수값으로 관리한다.

| 당월 목표 범위 | 달성 시 다음달 | 미달성 시 다음달 |
|:---:|:---:|:---:|
| 0~49 | +10 | 유지 |
| 50~99 | +15 | 유지 |
| 100 이상 | +20 | 유지 |

- **달성 판정**: 해당 월 1일~말일 기록의 마일리지 합산 >= 해당 월 목표
- **달성률 최대**: 100% (초과 달성해도 100%로 cap)

### 5.3 목표 자동 생성 방식

**lazy + 일괄 생성**: 아무 참여자가 프로젝트 페이지에 접속하면 전체 참여자의 당월 목표를 일괄 생성.

1. 프로젝트의 전체 참여자 중 `start_month <= 당월`인 사람 조회
2. 각 참여자에 대해:
   - 당월 `mileage_goal`이 이미 있으면 스킵
   - 이전 월 목표가 있으면 → 달성 여부 확인 → 갱신 규칙 적용
   - 이전 월 목표가 없으면 → `initial_goal`로 생성

### 5.4 목표 수정

- 해당 월 **1일~14일(포함)** 까지만 수정 가능. 15일부터 잠김.
- 현재 목표 **이상으로만** 수정 가능 (하향 불가)

### 5.5 목표 fallback

`mileage_goal`이 없는 월에는 `initial_goal`(참가 신청 시 설정한 값)을 표시/계산에 사용.

---

## 6. 참여 신청 및 보증금

### 6.1 참여 신청

- 앱 내 "참여하기" 버튼으로 신청 (제목과 그래프 사이에 위치, 미참여자만 표시)
- 신청 시 초기 목표 설정 (초보 50 / 고수 100 / 초고수 자유입력)
- 운영진이 보증금 입금 확인 후 참여 승인

### 6.2 보증금 구성

| 항목 | 금액 | 비고 |
|------|------|------|
| 월별 보증금 | 잔여 개월 수 × 1만원 | 환급 대상 |
| 참가비 | **2만원** (기본) | 환급 없음 |
| 참가비 (싱글렛 보유) | **1만원** (1만원 할인) | 환급 없음 |

- **잔여 개월 수**: `max(프로젝트 시작월, 현재 월)` ~ 종료월 (동적 계산, 최대 5개월)
- **회식비 풀 계산 시 참가비는 1인당 1만원 고정** (2만원 낸 사람의 나머지 1만원은 별도 적립)

**예시**: 5월 시작 + 싱글렛 미보유 = 5만(보증금) + 2만(참가비) = **7만원**
**예시**: 5월 시작 + 싱글렛 보유 = 5만(보증금) + 1만(참가비) = **6만원**

### 6.3 중도 참여

- 언제든 합류 가능
- 월 중 합류해도 해당 월부터 시작 (5월 10일 합류 → 5월부터)
- 중도 합류자도 합류 월의 전체 목표를 달성해야 함 (일할 계산 없음)
- 보증금은 잔여 개월분 기준 (합류 월 포함)

### 6.4 탈퇴/포기

- **중도 탈퇴 기능 없음** — 앱에서 탈퇴 버튼 없음
- 사실상 포기자 = 활동을 안 하는 사람. 달성률 0%인 월은 미환급 → 회식비 풀로 전환
- 환급은 9월 종료 시 일괄만, 예외 없음

### 6.5 승인 흐름

1. 앱에서 참여하기 → `project_participation` 생성 (`deposit_confirmed = false`)
2. 관리자가 `/admin/participations`에서 입금 확인 → `deposit_confirmed = true`

---

## 7. 환급 계산

### 7.1 월별 환급

```
월 환급률 = min(해당 월 달성 마일리지 / 해당 월 목표, 1.0)
월 환급액 = 월 환급률 × 1만원
```

- 초과 달성해도 환급률은 최대 1.0 (100%)
- **실전 기간만** 계산 (연습 기간 제외)

### 7.2 총 환급액

```
총 환급액 = 각 실전 참여 월의 환급액 합산
```

**예시**: 5개월 참여, 달성률 100%, 100%, 100%, 70%, 30%
→ (1.0 + 1.0 + 1.0 + 0.7 + 0.3) × 1만 = **4만원 환급**

### 7.3 환급 시점

- 9월 종료 시 일괄 환급

### 7.4 앱 표시

| 좌측 | 우측 |
|------|------|
| **확정 환급액** (당월 포함) | **회식비 지원 예상금액** (1인 상한) |

하단: **회식비 잔여 총액** (전체 풀), "내 지분: 참여 N개월 / 전체 N개월"

---

## 8. 회식비 계산

### 8.1 회식비 풀 구성

```
회식비 풀 = 전원의 (월별 보증금 - 환급액) 합산 + 전원 참가비(1인 1만원)
```

- 미환급 보증금에 포기자 보증금이 이미 포함됨 (달성률 0% → 전액 미환급)
- 싱글렛 보유 여부와 무관하게 참가비는 1인당 **1만원 고정**
- 연습 기간 목표는 제외

### 8.2 1인당 회식비 상한 (참여 개월 수 비례 지분제)

**전체 참여자** 대상으로 지분 계산 (실전 기간 개월 수 기준):

```
1인 지분 = 본인 참여 개월 수 (실전 기간)
1인 상한 = 회식비 풀 × (본인 지분 / 전체 참여자 지분 합)
```

**예시**: 풀 50만원, A(5개월), B(5개월, 불참), C(3개월) 회식 참석
→ 참석자 지분: A(5) + C(3) = 8
→ A 상한: 50만 × 5/8 = 312,500원
→ C 상한: 50만 × 3/8 = 187,500원

### 8.3 규칙

- 상한 초과분은 자비 부담
- 회식 후 잔여금은 **크루 회비로 귀속**

---

## 9. 크루 진행현황 그래프

### 9.1 축

- **X축**: 선택된 월의 1일 ~ 말일
- **Y축**: 마일리지(절대값) 또는 달성률(%)
- **토글 버튼**: "마일리지" / "달성률" (우측 상단)

### 9.2 데이터 표시

- **표시 대상**: 해당 월에 목표가 있거나 기록이 있는 참여자만
- **누적 마일리지**: 각 일자까지의 누적합
- **달성률**: `min(누적 마일리지 / 목표, 100)%` — 100%가 최대
- **목표 기준선**:
  - 마일리지 모드: 본인 목표값 수평 점선
  - 달성률 모드: 100% 수평 점선

### 9.3 툴팁

- 상위 5명만 표시 (값 내림차순)
- 본인이 상위 5명에 없으면 추가로 표시
- 나머지는 "외 N명"으로 축약

### 9.4 그래프 갱신

- 기록 입력/수정/삭제 후 `router.refresh()` + `refreshKey`로 즉시 반영

---

## 10. N월 진행현황 통계

그래프와 같은 border 블록 안에 위치. 그래프 모드(마일리지/달성률)에 연동.

| 항목 | 마일리지 모드 | 달성률 모드 |
|------|:---:|:---:|
| 참여 인원 | N명 | N명 |
| 평균 값 | **평균 마일리지** (km) | **평균 달성률** (%) |
| 달성 인원 | N / N명 | N / N명 |
| 환급 예상 총액 | O (실전만) | O (실전만) |
| 회식비 풀 | O (실전만) | O (실전만) |

- **참여 인원**: 해당 월 이전에 참가 신청한 전원 (`start_month <= 해당 월`, 목표 유무 무관)
- **평균 달성률**: 각 참여자의 `min(달성/목표, 1)` 평균 × 100
- **목표 fallback**: `mileage_goal`이 없으면 `initial_goal` 사용
- **그래프 모드 공유**: ChartModeContext (React Context)

---

## 11. 내 현황

| 항목 | 설명 |
|------|------|
| 당월 목표 | `mileage_goal` 조회, 없으면 `initial_goal` fallback |
| 현재 마일리지 | **선택된 월의 기록만** 합산 (해당 월 1일~말일) |
| 진행률 | `min(현재 / 목표, 100)%` |
| 기간 대비 달성률 | 진행률 / (경과일 / 총 일수) |
| 일일 필요 거리 | (목표 - 현재) / 잔여일. 달성 시 "달성 완료" 표시 |
| 프로그레스 바 | 진행률 시각화 |

---

## 12. 기록 입력

### 12.1 입력 항목

| 항목 | 타입 | 필수 | 비고 |
|------|------|------|------|
| 날짜 | Date | O | 기본값: 오늘 |
| 종목 | Select | O | 러닝 / 트레일러닝 / 자전거 / 수영 |
| 거리 | Number (km) | O | |
| 상승고도 | Number (m) | - | 수영 제외 전 종목, 선택 입력 |
| 이벤트 | Checkbox | - | 활성 이벤트 목록에서 복수 선택 |
| 후기 | Text | - | 한 줄 텍스트, 선택 |

### 12.2 입력 제한

- **미래 날짜**: 입력 불가
- **당월 기록**: 자유 입력
- **전월 기록**: 매월 3일까지만 입력 가능. 이후는 관리자만 가능.
  - 예: 6월 4일 → 5월 기록 입력 불가 (관리자 제외)

### 12.3 수정/삭제

- 본인 기록만 수정/삭제 가능
- 수정: Sheet(하단 시트)에 ActivityLogForm edit 모드
- 삭제: AlertDialog 확인 후 삭제
- 수정/삭제에도 입력과 동일한 시간 제한 적용 (전월 기록은 3일까지만, 이후 관리자만)
- 전월 기록 잠금 시 UI에서 버튼 비활성 + 안내 메시지

### 12.4 내 기록 목록

- **월별 필터**: 선택된 월의 기록만 표시
- **정렬**: 날짜 내림차순 (최신순)
- **초기 표시**: 5개, "더보기" 버튼으로 5개씩 추가
- **즉시 반영**: 입력/수정/삭제 후 `router.refresh()`

---

## 13. 운영진 관리 기능

### 13.1 대시보드 (`/admin`)

두 섹션으로 분리:

**일반**:
- 가입 승인 대기
- 활성 회원
- 이번 달 대회
- 전체 기록

**프로젝트**:
- 참여자 관리 (대기 N / 참여 N)
- 활성 프로젝트
- 활성 이벤트

### 13.2 참여자 관리 (`/admin/participations`)

**승인 대기 섹션**:
- 미승인 참여자 목록
- 각 항목: 이름, 초기 목표, 납부 금액 (보증금 + 참가비 상세), 싱글렛 할인 표시
- 승인/거절 버튼
- 승인 시 즉시 참여자 목록으로 이동

**참여자 목록 섹션**:
- 승인된 참여자 전체 목록
- 이름, 초기 목표, 시작월, 싱글렛 보유 여부, 납부 금액

### 13.3 이벤트 배율 관리 (`/admin/events`)

- CRUD: 이름, 배율, 시작일/종료일, 활성 토글
- 기존 기록 소급 변경 없음 (입력 시점의 배율 적용)

### 13.4 프로젝트 관리 (`/admin/projects`)

- CRUD: 이름, 시작월, 종료월, 상태(active/ended) 토글

### 13.5 설정 페이지

- 관리자에게만 "관리자 페이지" 단일 링크 표시 (`/admin`)

---

## 14. 기타 사항

- **시간대**: KST (UTC+9) 기준
- **달성 판정**: 소수점 포함 전체 정밀도로 비교 (49.9 < 50 = 미달성)
- **프로젝트 종료 후**: 9월 종료 후에도 기록/통계 열람 가능 (읽기 전용)

---

## 15. 데이터 모델

| 테이블 | 용도 | RLS |
|--------|------|-----|
| `project` | 프로젝트 정보 (이름, 기간, 상태) | 전체 SELECT, admin ALL |
| `project_participation` | 참여 정보 (회원, 프로젝트, 시작월, 보증금확인, 싱글렛) | 본인+admin SELECT, 본인 INSERT, admin ALL |
| `mileage_goal` | 월별 목표 (participation, 월, 목표값) | 본인+admin ALL |
| `activity_log` | 운동 기록 (participation, 날짜, 종목, 거리, 고도, 마일리지, 후기) | 본인+admin ALL |
| `event_multiplier` | 이벤트 배율 (프로젝트, 이름, 배율, 활성, 기간) | 전체 SELECT, admin ALL |
| `activity_log_event` | 기록-이벤트 연결 (다대다, 배율 스냅샷 저장) | 본인+admin ALL |

---

## 16. 파일 구조

```
app/(main)/projects/page.tsx          # 프로젝트 메인 페이지
components/projects/
  month-navigator.tsx                 # 월 네비게이터 (< N월 >)
  chart-mode-context.tsx              # 그래프 모드 Context (mileage/percent)
  crew-progress-chart.tsx             # 크루 진행현황 그래프 (클라이언트)
  crew-monthly-stats.tsx              # N월 진행현황 통계 (서버)
  crew-monthly-stats-client.tsx       # N월 진행현황 UI (클라이언트, 모드 연동)
  my-status.tsx                       # 내 현황 (서버)
  refund-status.tsx                   # 환급/회식비 (서버)
  my-activity-list.tsx                # 내 기록 목록 (서버, 데이터 fetch)
  my-activity-list-client.tsx         # 내 기록 목록 UI (클라이언트, 수정/삭제)
  activity-log-fab.tsx                # 기록 입력 FAB
  activity-log-form.tsx               # 기록 입력/수정 폼
  join-section.tsx                    # 참가 신청 섹션
  recent-reviews.tsx                  # 최근 후기
app/actions/
  mileage-run.ts                      # 서버 액션 (기록 CRUD, 목표, 참여)
  admin/
    manage-event-multiplier.ts        # 이벤트 배율 관리
    manage-participation.ts           # 참여 승인/거절/조회
    manage-project.ts                 # 프로젝트 CRUD
app/(info)/admin/
  page.tsx                            # 관리자 대시보드
  events/page.tsx                     # 이벤트 배율 관리
  participations/page.tsx             # 참여자 관리
  projects/page.tsx                   # 프로젝트 관리
lib/mileage.ts                        # 마일리지 계산, KST 날짜 유틸
supabase/migrations/
  20260327000000_mileage_run.sql      # DB 스키마
```

---

## 17. 디버그 날짜 (개발용)

개발/테스트 시 날짜를 오버라이드할 수 있음.

```
# .env.local
NEXT_PUBLIC_DEBUG_DATE=2026-05-25
```

- `lib/mileage.ts`의 `getKSTDate()`에서 체크
- `NEXT_PUBLIC_` 접두사로 서버/클라이언트 모두 적용
- 기록 입력 폼의 `max` 날짜에도 적용
- **배포 전 반드시 제거**

---

## 부록: 계산 예시

### 예시 1: 기본 기록

> 러닝 10km, 상승고도 200m, 비 올 때(×1.2)
> 기본: 10 + 2 = 12 마일리지
> 최종: 12 × 1.2 = **14.4 마일리지**

### 예시 2: 복합 이벤트

> 트레일러닝 15km, 상승고도 500m, 비(×1.2) + 모임(×1.1)
> 기본: 15 + 5 = 20 마일리지
> 최종: 20 × 1.2 × 1.1 = **26.4 마일리지**

### 예시 3: 환급 계산

> 5월 시작, 초보(50), 싱글렛 보유
> 납부: 5만(보증금) + 1만(참가비) = 6만원
> 결과: 5월 100% / 6월 100% / 7월 100% / 8월 80% / 9월 40%
> 환급: (1.0+1.0+1.0+0.8+0.4) × 1만 = **4.2만원**
> 미환급: 0.8만원 → 회식비 풀

### 예시 4: 회식비 배분

> 풀: 30만원
> 회식 참석자: A(5개월), B(4개월), C(3개월)
> 총 지분: 12
> A: 30만 × 5/12 = 125,000원
> B: 30만 × 4/12 = 100,000원
> C: 30만 × 3/12 = 75,000원
