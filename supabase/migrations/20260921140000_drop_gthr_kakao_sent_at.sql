-- 카톡 변경 공지의 10분 묶음 창을 걷어내면서 그 판정에만 쓰이던 컬럼도 내린다.
--
-- 묶음을 없앤 이유: 장소를 고치고 바로 또 고치는 건 사람이 헷갈리는 중이라는 뜻이라,
-- 그 사이 공지를 삼키면 톡방에 남은 마지막 안내가 틀린 값이 된다. 도배보다 그쪽이 나쁘다.
--
-- 값은 발송 시각 기록뿐이라 보존할 게 없다. 프로덕션엔 애초에 올라간 적이 없다
-- (20260921120000 은 dev 에만 적용됐다).
alter table public.gthr_mst
  drop column if exists kakao_sent_at;
