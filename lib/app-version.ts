/**
 * 화면에 보이는 앱 버전 — 설정 화면 "버전 정보".
 *
 * ⚠️ **git 태그와 다른 개념이다.** 이 저장소엔 버전이 둘 있다:
 *
 * | | 값(2026-07-29) | 누가 정하나 | 뜻 |
 * |---|---|---|---|
 * | git 태그 | `v0.41.0` | main 머지 시 GitHub Action이 자동 semver | **배포 일련번호** |
 * | 여기 | `v2.1.0` | 사람이 손으로 올린다 | **제품 버전**(사용자에게 말하는 세대) |
 *
 * 배포는 잦아서 태그가 빠르게 오르지만, 사용자에게 "2.1이 나왔다"고 말하는 단위는 훨씬 굵다.
 * 그래서 둘을 붙이지 않는다 — 붙이면 문구 한 줄 고친 배포에도 버전이 오른다.
 *
 * `next.config.ts`의 `NEXT_PUBLIC_APP_VERSION`(= `git describe`)은 **아무 데서도 안 쓰인다.**
 * 예전에 화면에 쓰려던 흔적으로 보이는데, 지금 화면이 원하는 건 제품 버전이라 맞지 않는다.
 * 지우려면 지워도 되지만 이 파일과 헷갈리지 않게 여기 적어 둔다.
 *
 * **올릴 때**: 사용자에게 알릴 만한 변화가 묶였을 때만. 올리면 설정 > 업데이트에 같은 번호로
 * 게시글을 하나 쓴다(둘이 어긋나면 "뭐가 바뀐 거지"를 확인할 방법이 없어진다).
 */
export const APP_VERSION = "v2.1.0";
